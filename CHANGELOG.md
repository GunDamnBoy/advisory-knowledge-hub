# CHANGELOG — 投顧知識庫儀表板・變更紀錄

**維護規則**：`AGENT_BRIEF.md` 與排程任務 `advisory-dashboard-daily` 的 prompt 是一組兩份，改任一邊都必須同步另一邊，並在本檔加一筆、寫清楚**為什麼改**。日期由新到舊。詳見 `MAINTENANCE.md`。

**本檔自 2026/08/08（第 14 次修訂）起自 `AGENT_BRIEF.md` 第 8 節拆出**——變更紀錄佔了 brief 的 31%，而每日排程第 0 步會完整讀 brief，等於每天花約 8,000 tokens 讀純歷史。拆出後日常執行不再讀本檔，維護時才讀。


---

## 版本總覽表

一行一版，供比較與回溯。**「來源」「子類別」「則數目標」是該版結束時的狀態**；影響檔案欄可用來判斷回溯時要看哪些檔。

| 版 | 日期 | 主題 | 來源 | 子類別 | 則數目標 | 深度卡 | 影響檔案 |
|---|---|---|---|---|---|---|---|
| 18 | 08-10 | 節點回顧・立場列・匯流條・旗標 | 25 | 15 | 95–125（週末 80–125） | 6–8 | check.py／metrics.py／brief／index.html／index.json／prompt |
| 17 | 08-10 | 時序化・thread・搜尋＋4 bug | 25 | 15 | 95–125（週末 80–125） | 6–8 | scripts×4／brief／index.html／**新增 search.html**／prompt |
| 16 | 08-10 | 週末模式、發稿日曆、探針策略 | 25 | 15 | 95–125（**週末 80–125**） | 6–8 | brief／prompt／check.py／preamble |
| 15 | 08-09 | 預篩改走列表頁、共用前言外部化 | 25 | 15 | 95–125 | 6–8 | brief／prompt／**新增 list_timestamps.js・subagent_preamble.md** |
| 14 | 08-08 | 每日 token 開銷結構性優化 | 25 | 15 | 95–125 | 6–8 | brief／prompt／**新增 CHANGELOG・scripts/** |
| 13 | 08-08 | 移除 Reuters、去重豁免、黃金換源 | **25**（−1） | 15 | 95–125 | 6–8 | brief／prompt／index.html |
| 12 | 08-06 | 新增 TrendForce 與 The Economist | **26**（+2） | 15 | 95–125 | 6–8 | brief／prompt／index.html |
| 11 | 08-06 | 台股官方源改走 API、讀取改輪詢、央行組特調 | 24 | 15 | 95–125 | 6–8 | brief／prompt |
| 10 | 08-05 | 十五組首輪實測的修正 | 24（KED→Korea Herald） | 15 | 95–**125** | 6–8 | brief／prompt／index.html |
| 9 | 08-04 | **子類別 8→15 組**、新增四家來源 | **24**（+4） | **15** | 95–115 | 6–8 | brief／prompt／index.html |
| 8 | 08-04 | 新增 SemiAnalysis、釐清 Reuters 攔截 | **20**（+1） | 8 | 80–95 | 8 | brief／prompt／index.html |
| 7 | 08-03 | 檢查腳本改用絕對路徑 | 19 | 8 | 80–95 | 8 | brief／prompt |
| 6 | 08-03 | 讓檢查腳本自己不會安靜失效 | 19 | 8 | 80–95 | 8 | brief／prompt |
| 5 | 08-03 | 修補第 4 次修訂當天留下的破綻 | 19 | 8 | 80–95 | 8 | brief／prompt |
| 4 | 08-03 | **時效改為滾動 24 小時窗口** | 19 | 8 | **80–95** | **8**（原 16） | brief／prompt |
| 3 | 08-03 | 修補 brief 與 prompt 的四處不同步 | 19 | **8**（原列 10） | 35–45 新卡 | 16 | brief／prompt |
| 2 | 08-03 | 讀取方法與瀏覽器綁定 | 19 | 8 | 35–45 新卡 | 16 | brief／prompt |
| 1 | 08-03 | 來源擴充 | **19**（+4） | 8 | 35–45 新卡 | 16 | brief／prompt／index.html |
| — | 08-02 | 改為封存制（外殼＋每日 JSON） | 15 | 8 | 3 日制、全站 126–155 | 16 | index.html／data |

### 關鍵參數的演變軌跡

- **時效**：3 日制（全站保留最近 3 個日期）→ **v4 滾動 24 小時窗口**（100% 當天新寫），這是整套系統最大的一次規則收緊。
- **子類別下限**：v1–v8 八組一律 ≥10 → **v9 十五組分級**（核心 ≥10／次要 ≥6／衛星 ≥3）→ **v11 央行特調 ≥8**。
- **則數目標**：35–45 新卡（3 日制）→ 80–95（v4）→ 95–115（v9）→ **95–125**（v10 起）。
- **來源**：15 → 19（v1）→ 20（v8）→ 24（v9）→ 26（v12）→ **25**（v13 移除 Reuters）。
- **架構**：規格與程式碼混在 brief 內 → **v14 單一來源化**（規格在 brief、程式碼在 `scripts/`、流程在 prompt、歷史在本檔）。

### 怎麼回溯

1. **每日產出**：`data/YYYY-MM-DD.json` 本身就是那天的完整快照，永久保留、不曾改寫。
2. **跨版趨勢**：`python3 scripts/metrics.py --groups --src`，即時從 data 重算，不需維護快照檔。
3. **規格與程式碼的舊版**：都在 git 裡（dashpush 每 180 秒自動 commit）。**不要跑 git 指令**（會留 index.lock 擋住推送），改用 Python + zlib 直接讀 `.git/objects`——解析 HEAD → tree → blob 即可，純讀取不產生鎖。
4. **排程 prompt 的舊版**：見 `prompts/`（v14 起才開始存；**v1–v13 的 prompt 已不可考**，因為 `update_scheduled_task` 是整份取代且不在 repo 內）。

---

## 逐版明細

### 2026-08-10（第 18 次修訂 · 盯盤節點回顧、五資產立場列、姊妹庫匯流條、異常旗標）

**動機**：使用者在第 17 次修訂（活化 thermo／snap 兩個沉睡資產）之後問「還可以新增什麼」，核准了四個提案全做。共同設計原則：**index.json 從「檔案清單」升級為系統的「跨日記憶」**——writer 每天本來就要讀它，把昨天的 watch／pulse／threads 鏡像放進 entry，跨日功能就全部不用打開任何舊日檔。

- **盯盤節點回顧（`overview.watchReview`，必填）**：把昨天的 watch 逐條標 `應驗`/`落空`/`未決`＋一句話附數字。**這是本站與一般新聞摘要的分水嶺——拋出去的預測要回頭對答案**，跨月累積就是一份可引用的預測紀錄（「我們 X 日提示的節點已應驗」）。check.py 強制：前一版 watch 非空則回顧不可空；規格明訂「模稜兩可就是未決，落空的 note 最有價值」。前端渲染在盯盤時程之後，附「N 應驗 · N 落空」統計。
- **五資產立場列（`overview.pulse`，必填）**：美股／美債／美元／黃金／原油固定五鍵，`dir`∈偏多/中性/偏空＋≤30 字理由。**每日版的迷你 House View**：月度 House View 可直接 diff「本月翻轉了幾次、何時翻的」。前端翻轉時自動標「翻轉·昨偏空」（比對 index 昨日 entry，零額外請求）；metrics.py 新增 `--pulse` 歷史矩陣。規格要求翻轉必須有意識、note 講清楚——防止立場天天漂移。
- **姊妹庫晨間匯流條（純前端，writer 零負擔）**：總覽底部三格——AI 泡沫監控（綜合溫度＋台股過熱度）、Podcast 知識庫（今晨集數＋節目）、每日五圖（昨日標題）。**只在最新一天顯示**，歷史封存保持純快照；任一站掛掉該格自動隱藏。三站介面經實測確認：泡沫監控無公開 data JSON，但頁內有 `<script id="dashboard-data" type="application/json">` 內嵌完整資料（`composite`／`tw.heat`／`meta.built`），fetch HTML 後正則抽出即可；Podcast 與五圖都有標準 `data/index.json`。
- **異常旗標（純前端，writer 零負擔）**：index entry 新增 `snap` 數值鏡像，前端累積 ≥10 天後、當日 |chgPct| 創期間新高自動標「⚑ 近 N 日最大波動」。門檻 10 天是為了避免序列太短時天天觸發；約 8/21 起自動亮起。
- **index.json entry 擴為五個跨日記憶欄位**：`thermo`／`threads`／`watch`／`pulse`／`snap`。歷史十天已回填 `watch` 鏡像（8/11 首跑就能寫回顧）；`pulse`／`snap` 自 8/11 起累積。
- check.py v18 檢查與 v17 同日（2026-08-11）生效：pulse 五鍵與方向詞、watchReview 完整性與 verdict 白名單、index 五欄位。單元測試：完整假 8/11 檔 exit=0；缺 pulse、缺回顧、verdict 亂寫各自被具名攔截。

**每日撰寫負擔評估**：watchReview 約 5 條×一句話、pulse 五行——素材 writer 本來就讀過，估增量 <2% 的輸出 token；匯流條與旗標為零負擔。**若 8/11～8/13 實跑顯示品質不穩（回顧敷衍、立場亂翻），優先修規格用語而不是撤功能。**

**影響檔案**：scripts/check.py、scripts/metrics.py（--pulse）、AGENT_BRIEF.md §3.5/§4、index.html、data/index.json（回填 watch）、排程 prompt（v18，快照 prompts/2026-08-10-v18.md）。

### 2026-08-10（第 17 次修訂 · 時間序列化、主題連續劇、全站搜尋＋四個程式碼 bug）

**動機**：使用者要求全面重審程式碼並構思新功能。審視發現**封存裡已經埋著兩個沒被利用的資產**——`overview.thermo` 有 0–100 的風險溫度數字、`overview.snap` 有每日全套報價，每天都在寫、但從來沒被跨日串起來。同時逐行審出四個真 bug。

**四個 bug 修正**（全部只動 `scripts/`，附驗證）：

1. **`list_timestamps.js` 會把 MoneyDJ 整站壓成一條**：連結正規化只留路徑，但 MoneyDJ 的文章 ID 在查詢字串（`NewsViewer.aspx?a=GUID`），全站文章共用同一路徑、被 `seen` 去重成 1 條。順帶發現文章連結的正則沒有 `i` 旗標，`/News/`（大寫）根本比對不到。修：查詢字串帶 ≥8 碼 ID 時保留、正則加 `i`。
2. **`check.py` 歷史重跑時去重比對方向錯誤**：`prevmeta` 取「最新的非當日版本」，對 8/06 重跑會拿 8/10 來比。修：取「小於該日的最大日期」。修後五天重驗，8/06 正確比對 8/05。
3. **naive `ts` 會讓 check.py 崩潰**：`fromisoformat` 接受不帶時區的字串，與 aware 的窗口比較時 TypeError 未捕捉。修：`T()` 檢查 `tzinfo`，naive 一律視為「缺/壞 ts」。單元測試確認：壞卡被列名、exit=1、不崩潰。
4. **MoneyDJ 每天三則假警報**：「疑似列表頁」啟發式剝掉查詢字串後 `NewsViewer.aspx` 像列表頁。修：與 #1 同一判準。修後 8/10 重驗警告歸零。

**低成本強化**：check.py 加窗口起點驗證（必須是前一日 07:00 台北，抓排程漂移）、頂層 `date`／`cards` 一致性檢查；metrics.py 補 v15/v16/v17 里程碑、`--groups` 對週末列改用週末下限（先前拿平日下限量週末、趨勢表誤導）、`--csv` 加 thermo 欄；read_article.js 回傳 `title`、全頁 p 保險絲改為「抓到更多才換」。

**三個新功能**：

- **時間序列化**：`thermo.level` 規格化為 0–100 整數字串（8/11 前有幾天寫成散文，序列因此有斷點）；`snap` 每格加 `num`／`chgPct` 數值欄；`index.json` 當日 entry 加 `thermo` 與 `threads`（歷史十天已回填 thermo，6 天有值）。前端：日期列顯示歷史溫度、溫度條加數值標記。**下游 House View 與 convergence-weekly 從此可以直接吃數字，不用再從 HTML 字串裡摳。**
- **主題連續劇（thread）**：卡片選填 `thread` 代號（2–16 字，跨日沿用同一代號，一天 3～6 張為宜），總覽顯示「延續中的主題 · 第 N 天」（由 index.json 計算、零額外請求），metrics.py `--tags` 一併統計。convergence-weekly 以此欄直接追主題，不用自己做跨日字串比對。
- **全站搜尋（search.html）**：前端載入全部封存做即時搜尋（標題／內文／標籤／thread，含來源與分組篩選、關鍵字高亮、分批載入邊載邊搜）。**每日快報從此升級成可查詢的知識庫**；純前端、不需要每日維護。
- 另加 metrics.py `--tags`：近 8 天標籤頻率矩陣，看題材升溫／退燒。

**check.py 的 v17 檢查自 2026-08-11 起生效**（thermo 整數、snap.num、index 欄位、thread 格式），歷史檔不回溯套用。8/11 是第一輪實跑，盯：writer 有沒有正確填 num/chgPct、thread 代號有沒有沿用。

**觀察項（未動）**：華爾街見聞 8/10 首次全站 0 則（重度 SPA 未渲染、五種入口全試、sitemap 404）。比照 NYT 規則先觀察，連三天再處置。

**影響檔案**：scripts/check.py、scripts/metrics.py、scripts/list_timestamps.js、scripts/read_article.js、AGENT_BRIEF.md §3.5/§4、index.html、**新增 search.html**、data/index.json（回填）、排程 prompt（v17，快照 prompts/2026-08-10-v17.md）。

### 2026-08-10（第 16 次修訂 · 週末模式、發稿日曆、探針策略）

**動機**：8/10 那輪用量 2,585 萬（8/09 是 2,400 萬），子代理佔比從 67.3% 升到 **87.3%**，跑了**四輪補位**。但拆開看，成本不是被採集效率驅動的，**是被「週末」驅動的**——窗口涵蓋週日，而 **Fierce Biotech（最新 8/07）、FDA（8/07）、OGJ（8/07，週末不發稿）、TrendForce 新聞中心（8/05）、IBD（最新 8/08 06:58）在窗口內全部零新文**。生技組三個指定來源同時開天窗，系統為了湊平日下限開了 J、K 兩輪補位橫跨 15 個以上來源，最後補到 4 則、**全偏公共衛生與健保政策，沒有一則新藥或臨床試驗的硬素材**。四輪補位合計 585 萬、佔全輪 **22.6%**，J 組單則成本 91 萬是 F 組（台股，16 萬）的 5.7 倍。

**平日對照更清楚**：8/06～8/08 是 122～123 則、25～26 家來源、1 輪補位；週末兩天是 105～106 則、19～20 家、1～4 輪。**系統不知道今天是週末，還在用平日配額要求週末素材。**

- **新增第 4.1.1 節「週末模式」**，`check.py` 自動判定。次要組 ≥6→**≥4**、衛星組 ≥3→**≥2**、全站 95–125→**80–125**；**核心四組（美股／AI／台灣／央行）不放寬**——它們週末仍達標，放寬等於無故降品質。
  **判定看窗口起點那一天，不是終點**：窗口是「前一日 07:00 → 當日上午」、主體是前一日。第一版寫成 `frm or to` 落在週末就算，結果 8/08 那輪（終點週六、主體週五、123 則）被誤判為週末；改為只看 `frm` 後，8/06～8/08 判平日、8/09～8/10 判週末，與實際素材量吻合。
- **前言新增第 3.6 節「來源發稿日曆與黑名單」**：列出週末不發稿的六家與替代路線；**明列 Reuters、KED 為已移除來源**——8/10 那輪 I、K 兩組各自讀到 Reuters，**4 則素材全數捨棄**（含 2 則日銀意見摘要），日本組因此少了緩衝。**這是 8/09 寫 preamble 時的疏漏**：只說了要讀哪些，沒說不要讀哪些。
- **新增探針策略**：列表頁取不到時間戳時（`withTs: 0`），先讀最上面 1 篇看時間，逾期且該頁為時間排序就**整站跳過**。8/10 E 組把 IBD 6 篇全部完整讀完（每篇 21～50 段、4,200～9,600 字）才發現最新一篇是 8/08 06:58、**全部落在窗口外**——一支探針就能省掉整組。
- **Nikkei Asia 的 `/Economy` 是主題精選不是時間排序**：8/10 I 組 15 篇配額全部用罄卻 0 篇落在窗口內，約 100 萬 token 歸零。改用首頁動態流或 `/Business` 最新欄。
- **補位輪次上限 2 輪**：第三輪之後邊際效益極低，撈到的多半是勉強沾邊的題材。兩輪後仍有缺額就接受並在 `run` 欄具名記錄。
- **關於「列表頁預篩有沒有省 token」**：**沒有，總量還升了 7.7%**——但它本來就不是為了省 token。它換到的是**零風控與 Bloomberg 回歸**（8/09 成卡 0 則 → 8/10 恢復 13 則，全輪文章頁請求控管在 16 篇）。這筆交易划算，不要因為總量沒降就回頭。

### 2026-08-09（第 15 次修訂 · 預篩改走列表頁、共用前言外部化）

**動機**：8/09 那輪 Bloomberg 成卡 0 篇，`run` 欄記錄的原因是「A 組在預篩時間戳階段對 Bloomberg **連續發出約 35 次 fetch**，觸發網路層風控」——而**開場的可用性測試原本是通過的**（23 段／5,208 字）。也就是說**不是來源本來就擋，是採集方式把它惹毛的**。Bloomberg 在此之前八天分別交出 24、27、27、14、20、19、15、17 則，一直是全站最大來源。

同日 09:50 的維護實測確認了這個判斷：**Bloomberg 列表頁完全正常**（15,729 字元、101 條連結、無驗證頁），**但文章頁仍回傳「Are you a robot?」（4 段／464 字，與排程當下數字完全相同）**，距排程結束已 40 分鐘仍未恢復。**「列表頁通、文章頁全擋」就是風控被觸發的特徵。**

- **新增第 1.1.1 節：預篩時間戳一律從列表頁做。** 新建 `scripts/list_timestamps.js`——列表頁一次取回全部「連結＋時間戳」（含 `<time>`、`data-timestamp`、相對時間三層備援），篩出窗口內的再點進去讀正文。**單一來源文章頁請求上限 15 篇。** 這同時解決可用性與成本兩個問題：預篩請求從 35 次降為 1 次。
- **明確區分兩種風控，處置方式不同**：Reuters 那種「進門就擋」（八天七天零產出）該移除；**Bloomberg 這種「自傷型風控」該改採集方式**。也寫明觸發後不要靠重試解決（8/09 重試四次、另開新分頁均無效）。
- **NYT 標為「阻擋會自癒」。** 8/09 四步診斷全跑完仍是 0 字元，但同日 09:50 實測 **37 段／11,271 字、時間戳正常、3 秒穩定**，「從列表頁點進」與「直接 navigate」兩種方式都通。**這是第二次同一模式**（8/05 全 0 → 8/06 恢復），中間 8/06～8/08 都有產出。結論：四步診斷照跑、記錄、當日放棄即可，**不要為它改動任何規則**。
- **新增 `scripts/subagent_preamble.md`（共用前言外部化）。** 9 份派工提示中「讀法、合規紅線、回報格式」三段幾乎逐字重複；收進單一檔案後，派工只帶差異部分。依 8/09 的 token 報告，派工提示佔全輪 5.6%，且會連帶降低每個子代理每回合的快取讀取量。
- **新增第 1.1.2 節「各來源的已知眉角」。** 這張表原本**只存在於排程 prompt 裡**，是規格散落在流程檔的殘留；依第 14 次修訂確立的單一來源原則移進 brief，prompt 改為指向本節。同時補上 Bloomberg 與 NYT 兩列。
- **prompt 加一條：派補位工時要帶「已收 URL 清單」。** 8/09 補位一交回 6 則有 4 則重複、補位二與 B 組撞同一篇，實測浪費約 60 萬有效 token。
- **同日 10:35 複測：Bloomberg 已恢復**（同一篇文章從 4 段／464 字回到 **23 段／5,208 字**，另測一篇不同文章 17 段／5,031 字，排除快取）。**風控約 2～3 小時自行解除**，但恢復時間落在當日窗口尾聲、救不回當天產出——**重點永遠是不要觸發**。
- **複測時發現 `list_timestamps.js` 的 DOM 三層備援在 Bloomberg 上會全部落空**：它的列表頁 33 條連結**沒有任何 `<time>` 或相對時間**。已補上第四層——掃 Next.js 的 `window.__NEXT_DATA__`，實測一次取回 **62 篇的 slug＋標題＋發布時間**。**若沒發現這點，「改走列表頁預篩」這個方案在最需要它的那家反而失效。**
- **另一個支持列表頁預篩的理由**：部分 Bloomberg 文章頁的 `article:published_time` 是**空的**（8/09 實測 Korea Volatility 那篇），`ts` 只能靠列表頁的 `__NEXT_DATA__` 補。
- **⚠ 修正當日自己製造的迴歸：眉角表移錯地方。** 把「各來源眉角」從排程 prompt 移進 brief 第 1.1.2 節之後才發現——**採集員只拿到「共用前言＋兩支腳本＋配額」，根本不讀 brief**，等於 Korea Herald 的 `/Business`、Mint 的全頁 `p`、Bloomberg 的 `__NEXT_DATA__` 這些資訊對真正要用的人消失了。已改放進 `subagent_preamble.md` 第 3.5 節（10 條，含各組專屬提醒），brief 第 1.1.2 節改為指向該檔。
  **教訓**：判斷一份知識該住在哪，要看**誰在執行的當下需要它**，不是看它「感覺上像規格還是像流程」。單一來源原則的動機是對的，但「單一」指的是不要有兩份副本，不是「一律往 brief 集中」。
- **關於 8/09 的 token 報告**：該報告誠實指出「第 14 次修訂打到的是全輪 4.0% 的固定開銷，真正的成本 67.3% 在採集端」。本次修訂正是往採集端下手——**列表頁預篩同時砍請求數與回合數，是目前唯一能同時改善成本與可用性的槓桿**。

### 2026-08-08（第 14 次修訂 · 每日 token 開銷結構性優化）

**動機**：本系統每天自動跑，每一輪的固定開銷約 33,000+ tokens，其中三個大頭都是結構問題：brief 的 31% 是變更紀錄（每天被完整讀但純屬歷史，≈8,100 tok）；排程 prompt 與 brief 大量重複（端點表、QUOTA、檢查腳本、眉角表各兩份，而 brief 反正每天全文讀）；檢查腳本內嵌在兩份文件裡、每天還要重新打字進 bash。

- **變更紀錄拆出為 `CHANGELOG.md`**。brief 第 8 節只留指標，每日排程明文不讀本檔。省 ≈8,100 tok/天。
- **檢查腳本外部化為 `scripts/check.py`（唯一權威版本）**，帶出口碼（0＝通過），QUOTA／SRCOK／DEDUP_EXEMPT 三張表都在裡面。brief 與 prompt 各只留一行指令。**同時消滅了「兩份腳本不同步」這個歷史上最大的漂移源**——它從第 4 次修訂起漂過三次。
- **JS 讀法外部化為 `scripts/read_article.js`**，主 agent 每天讀一次、逐字轉發給 7 個 subagent。
- **排程 prompt 重寫為精簡工作流**（16.5k → 6.6k 字元）：步驟順序＋brief 節次指標，移除所有與 brief 重複的規格表與歷史敘事。**單一來源原則**：程式碼在 scripts/、規格在 brief、流程在 prompt。
- **合計每日固定開銷降約 40%**（≈33k → ≈20k tok），且往後改程式碼不必再同步兩份文件。
- **同日稍後補上回溯基礎建設**（應使用者「詳細記錄每一版本更動、方便比較與回溯」的要求）：新增 **`scripts/metrics.py`**（掃 data/*.json 即時重算跨版趨勢、各組緩衝、來源貢獻矩陣，不需維護快照檔；下限表以 `check.py` 為唯一權威，`import` 取得而非再抄一份）、本檔**版本總覽表**（一行一版的關鍵參數與影響檔案）、**`prompts/` 目錄**（排程 prompt 的存檔副本——正本不在 repo 也不在 git，`update_scheduled_task` 又是整份取代，**v1–v13 因此已不可考**，v14 起開始存）。順帶把 `check.py` 的執行邏輯包進 `main()`，讓 `metrics.py` 能安全 `import QUOTA` 而不觸發它執行。
- **過程中的事故與教訓（已寫進 MAINTENANCE 已知坑）**：批次精簡時一支帶 `re.S` 的貪婪正則把 brief 從 35k 字元吃到剩 5.7k；靠 Python + zlib 直接解析 `.git/objects`（純讀取、不產生鎖）從 dashpush 的自動 commit 救回。防範規則：替換前量測 match 長度、用精確錨點、改完驗證總長。

### 2026-08-08（第 13 次修訂 · 移除 Reuters、去重豁免、黃金換源）

- **移除 Reuters（本次最主要的決定）。** 依據是**實際排程的產出紀錄**：八個版本裡只有 8/05 拿到 6 則，其餘七天全部零產出（07/30、08/02、08/03、08/04、08/06、08/07、08/08），命中率 1/8。DataDome 反自動化風控在真實採集中幾乎全時生效。
  **這次同時修正了一個判斷方法的錯誤。** 8/04 那天曾寫下「屬間歇性、不要誤判為永久失效」，依據是「同日稍晚以同一台瀏覽器重測即完全恢復正常」——但**那次重測是在維護對話裡手動做的**。手動能通、自動被擋，本來就是反自動化風控的預期行為；**用手動測試的結果去推論自動流程的可用性是錯的**。要判斷來源去留，看的必須是它在實際排程中的產出紀錄。這條方法論已寫進第 1.1 節。
  `reuters` 的 CSS、`BADGE`、`SRCOK` 永久保留（8/05 那 6 則歷史卡片要渲染、舊檔重跑檢查不能誤報），只從 `SRCBAR` 與來源清單、D 組移除。**移除代價幾乎為零**——那七天的缺口本來就由 Tom's、OGJ、TrendForce 與 B/E 兩組實際承擔著，8/08 能源 7 則、黃金 4 則（含 2 則媒體端）都已達標。來源由 26 家減為 25 家。
- **新增跨版去重的豁免名單 `DEDUP_EXEMPT`。** TrendForce 報價頁的網址每天不變，8/08 撞上跨版去重被擋下；FRED、SPDR、CME、TWSE／TPEx、MOPS 全部同理，**這是結構性問題、往後每天都會發生**。這些頁面**內容每天都變、只有 URL 相同**，而去重規則的本意是「同一篇報導不要連兩天成卡」——語意上就該豁免。豁免同時套用在跨版比對與本版內拆卡判定。
- **黃金保底卡換源**：`gold.org/goldhub/data` 的 ETF 流向**要求登入**，8/08 執行者依合規紅線未建帳號、未取得（處置正確）。改用兩個免登入源——**SPDR Gold Shares（GLD）每日持倉**（資金流向代理）與 **COMEX 黃金庫存**（實體交割），兩個角度互補。**保底卡的意義就是「每天一定拿得到」，靠需要登入的來源等於沒有保底。**
- **Barron's 補上第四種版型的選擇器。** `div[class*="paragraph"]` 併入標準片段——8/08 那篇 Investor Circle／Technical Analysis 版型的 Palantir 報導，用既有選擇器只抓到 3 段／265 字、**又是「看起來就像被擋」**，換了之後 20 段／4,629 字。**這是 Barron's 第四次以不同形式製造同一種誤判**（Sign In 判斷 → 選擇器 → 等待時間 → 版型差異）。標準新聞版那 8 篇則全部一次到位，`meta[name="article.published"]` 本輪一次都沒用到、改標為備援。
- **Barron's 留任觀察的結論：留。** 輪詢讀法有效，8 篇標準版型全部一次到位（8–22 段／1,753–5,705 字）；唯一的例外是版型問題而非付費牆，且已修。

### 2026-08-06（第 12 次修訂 · 新增 TrendForce 與 The Economist）

**動機**：這次原本是要移除 Barron's，檢視後決定先留。**Barron's 並不是讀不到**——8/06 實測 8 篇中 7 篇完整取得，六天累計貢獻 27 則、在 8/06 那天的 23 個來源裡排第 7。它的問題是**連續三次以不同形式製造「看起來像被擋」的誤判**（8/03 用導覽列 Sign In 判斷、8/05 選擇器沒對上、8/06 等待 4 秒不足），而這三個坑已於第 11 次修訂全部補上（輪詢讀法、`p[class*="Paragraph"]`、`meta[name="article.published"]`）。**先觀察一週新讀法是否穩定，再決定去留。**

改為新增兩家補當前的實際弱點：

- **TrendForce 集邦（`tf`，免費，歸 D 組）**。動機是 **DRAM／NAND 價格至今全部靠二手轉引**——媒體引用的數字往往落後一至兩週，而且經常沒寫清楚是合約價還是現貨價。TrendForce 是這些報價的原始發布方。主要餵「AI 與半導體」（核心組 ≥10，8/05 曾剛好卡在 10 則零緩衝，是目前最脆弱的核心組），次要餵「能源與原物料」（功率半導體、太陽能供應鏈）。**以中文站 `trendforce.com.tw` 為主，英文站內容較少。** 已在 brief 寫明報價卡必須標清楚合約價／現貨價、DDR4／DDR5、顆粒／模組、月度／週度——**那正是二手轉引最常搞混的地方，也是收這家的全部意義**。
- **The Economist（`econ`，付費訂閱，歸 B 組）**。它提供的不是速報而是**解釋框架**，對每月 House View 的歐洲、地緣政治、央行三節價值最高。**新增第 1.3 節處理它的週刊節奏**：週刊正文於台北時間週四晚至週五凌晨整批上線（出刊當天窗口內會一次出現十幾篇，挑 2～4 篇成卡，適合寫深度卡）、線上專文平日 0～3 篇、**The World in Brief 每日早報不得單獨成卡**（它是各家新聞的彙整，不是原創報導，與本站定位重複）。比照 SemiAnalysis：**窗口內沒有新文不記為降級，也絕對不要收窗口外的舊文。** 另註明它的 `ts` 是 UTC、換算台北要加 8 小時，且**優先挑 Leaders／Finance & economics／Briefing 這幾版的分析，Business 版的公司報導可略過**（其他來源已覆蓋）。
- **來源由 24 家增為 26 家**；`index.html` 補上 `.b-tf`／`.b-econ` 的 CSS、`BADGE` 與 `SRCBAR`，檢查腳本 `SRCOK` 白名單同步加入 `tf`／`econ`。
- **子類別來源對照表新增「AI 與半導體」一列**（Tom's Hardware、SemiAnalysis、TrendForce、Bloomberg、FT），歐洲那一列補上 The Economist。

### 2026-08-06（第 11 次修訂 · 台股官方源改走 API、讀取改輪詢、央行組特調）

8/06 那輪：**122 則、十五組全數達標、五項歸零指標全為 0、10:40 完成**，NYT 恢復正常（四步診斷第 1 步即成功，8/05 那次應是暫時性渲染失敗）。以下是該輪暴露的問題與處置。

- **台股官方數據全面改走 JSON／CSV 端點**（第 5 節第 3 點）。MOPS 月營收與法說會兩頁**連續三輪被重導至新版 SPA**、櫃買指數**連續三輪未取得**（官網一次重導一次 404，台媒盤後彙整也沒載）。本次實測驗證出六個可用端點並寫進 brief，其中 **`https://www.tpex.org.tw/www/zh-tw/afterTrading/tradingIndex?date=YYYY/MM/DD&response=json` 解決了櫃買指數這個長期缺口**。另外兩個發現值得記住：**TWSE OpenAPI 沒有三大法人與融資融券**（只能走 `rwd` 端點），**也沒有法說會專屬端點**（只能從每日重大訊息 `t187ap04_L` 過濾「符合條款＝第12款」）。
- **讀取由「固定等 4 秒」改為「輪詢至段落數穩定」**（第 1.1 節）。Barron's 那篇 AMD 報導等 4 秒只有 2 段／318 字、**看起來就像被擋**，補等 5 秒後變 10 段／2,563 字。**Barron's 已經連續三次以不同形式製造誤判**——8/03 用導覽列 Sign In 判斷、8/05 選擇器沒對上、8/06 等待不足。固定秒數治不了根本，改為輪詢到段落數連續兩次不變（上限 12 秒）。
- **新增「個別來源的已知眉角」對照表**（第 1.1 節）：Barron's 時間戳在 `meta[name="article.published"]`；**Mint 的段落要用全頁 `p`**；**Korea Herald 列表頁必須用 `/Business`，`/section/business` 是殘頁**，且其 `article:published_time` 時區是 **+09:00、換算台北要減 1 小時**；IBD 只能 `navigate`；STAT 付費文扣掉記者簡介後只剩約 3 段。JS 片段也加了「段落數 <8 時退回全頁 `p`」當保險絲。
- **央行組下限由 10 特調為 8，並寫明取材範圍**（第 4.1 節、第 5 節）。8/06 該組 10 則剛好達標、零緩衝，原因是 Cook 談話與財政部再融資被多家媒體重複報導，依「一件事不開兩張卡」合併後可用素材大幅縮水。**這是結構性的——央行事件天然會被大量重複報導，去重後留下的獨立事件本來就少，光叫人「多讀」沒用。** 因此兩手並進：下限降為 8 留緩衝，同時明列該組應涵蓋的六個取材面向（各國央行不只 Fed、公債標售、殖利率曲線與利差結構、利率衍生品定價、匯率政策、財政與貨幣交界），並釐清「同一事件的不同市場反應可分開成卡，但同一事件的不同角度不可」。A 組配額 ≥6→≥5、C 組 ≥4→≥3。
- **`about.access` 不再寫合規聲明**（第 3.5 節）。依使用者指示移除「均在使用者本人訂閱、且已登入的 Chrome 中合法讀取，未使用任何繞過付費牆的手段」那句——這個資料庫是內部參考用途，頁面上不需要對外聲明。**第 2 節的執行層規則完全不變。**
- **新增第 2.1 節「瀏覽器擴充套件的干擾」**。8/06 在 statnews.com 偵測到 Bypass Paywalls Clean 注入 archive 鏡像站字串（當輪處置正確：未點擊、換篇、如實記錄）。寫進 brief 的理由**不只是合規，更是資料可信度**：這類擴充套件會自動改寫頁面，在 statnews 留下了可見字串所以被抓到，但**在 Bloomberg、WSJ、FT 上若生效不會留痕跡**，會讓「內文量標準」判定的「完整取得」無法分辨內容來自訂閱還是擴充套件——**而這種污染沒有任何跡象可循**。
- **黃金組的品質降級記錄**：8/06 該組 4 則**全部是官方數據卡**（WGC、CME），缺媒體端金價與礦業股報導，起因是 Reuters 當輪階段性被擋。這是當日最主要的品質降級點，已在降級段落補上「Reuters 被擋時黃金組改由 MarketWatch 與 Bloomberg 商品版面補位」。

### 2026-08-05（第 10 次修訂 · 十五組首輪實測的修正）

8/05 是十五組結構的第一次實測，結果比預期好：**114 則、十五組全數達標、五項歸零指標全為 0、09:27 完成**。第 9 次修訂的兩個責任指派立刻見效——**信用債由 0 則變 8 則、歐洲由 3 則（且都只是順帶提及）變 9 則以歐洲為主體的報導**。以下是那一輪暴露出來的問題。

- **補上「央行、利率與匯率」的採集責任歸屬（本次最重要）。** 第 9 次修訂把日圓／BOJ 從央行組移到日本組，卻**忘了指派誰負責央行組**——七組分派表裡它一次都沒出現。8/05 那輪七組回報後央行組只湊到 4 則（下限 10），必須加開一輪補位才補到 11 則。**素材從來不缺，缺的是有人負責。** 現改為 A 組（Bloomberg＋WSJ）負責政策面 ≥6、C 組（CNBC＋MarketWatch＋華爾街見聞）負責市場數據面 ≥4，並在第 5 節加上硬規定：**分派表必須涵蓋全部十五組，開工前先核對一次。** 黃金同樣只到 5 則、同樣靠補位，因為 D 組同時扛能源、AI 與黃金，已明確標註 ≥3 的責任。
- **修正 Barron's 的內文選擇器（重要，會造成誤判）。** brief 第 1.1 節原本的 `article p, main p, [class*="ArticleBody"] p` 在 Barron's 上只抓到 2 段／419 字，**看起來就像被付費牆擋住**；實際上 Barron's 的段落 class 是 emotion 動態雜湊、不在 `article`／`main` 底下，改用 `p[class*="Paragraph"]` 可取得 22 段／4,444 字。**8/03 才因為讀取方法誤判整家 Barron's 一次，這是同一個坑的第二種形式。** 已把該選擇器併入標準片段，並加上一條原則：**段落數少的時候先懷疑選擇器沒對上，再懷疑被擋。**
- **新增「`fetch()` 取 HTML ≠ 實際開頁」**（第 1.1 節 3.5 點）。IBD 實測用 `fetch()` 只拿到付費牆前導段（7 段／987 字），必須真的 `navigate` 才有完整內文（27 段／5,939 字）。**掃列表可以 `fetch`，讀正文一律 `navigate`。**
- **brief 第 1.1 節補上 Substack 選擇器**（`.available-content p, [class*="markup"] p`）。這是第 8 次修訂只改了排程 prompt、沒同步 brief 留下的既存不同步。
- **KED Global 換成 Korea Herald。** KED **對未登入讀者只提供導言**（`section.news-body` 只有 255～360 字元、`<p>` 標籤 0 個），而且**頁面上沒有任何攔截字串**——這種安靜的截斷特別危險：內文量標準會判定為被擋，但只看「有沒有訂閱提示」的執行者會誤以為讀到了。Korea Herald 全文免費，改由它當韓國主力。**`ked` 徽章代碼保留不刪**：8/05 那版有 3 則 KED 卡片，移除 CSS 與 `BADGE` 會讓該日封存檔渲染出空徽章。**舊代碼一律只從 `SRCBAR` 移除、CSS 與 `BADGE` 永久保留，`SRCOK` 白名單也留著**，否則拿舊檔重跑檢查會誤報。
- **STAT News 定位下修為輔助。** 免費新聞篇數偏少（窗口內 8 篇有 5 篇是 STAT Plus），**每天 1～2 則就算正常**。生技健護的支柱是 Fierce Biotech（8/05 實測 7 篇全數完整取得）。
- **新增 NYT 的四步診斷梯度。** 8/05 那輪 11 篇實測 `innerText.length` 全為 0、`title` 停在 `"nytimes.com"`，是頁面完全沒渲染；但使用者同日手動開啟完全正常，**所以不是帳號或訂閱問題**。判定硬阻擋前要依序試完：等 8–10 秒、先開首頁再點進、換分頁、確認是否整站。四步都失敗才記為當日不可用。
- **TWSE 三大法人改用當日永久連結** `BFI82U?dayDate=YYYYMMDD`。表單頁網址每天相同，直接當 `url` 會撞上跨版去重——8/05 就是這樣被擋、改成當日連結才過。**其他每日重複抓的官方數據頁同理，能帶日期參數就帶。**
- **全站上限由 115 提高為 125。** 8/05 素材總量 149 則、因上限汰除 35 則，而完成時間 09:27、遠早於 12:30。素材與時間都有餘裕，多留 10 則給下游的週報與月報用。
- **完成時間目標維持 12:30。** 連兩輪都在 09:30 前完成，但週末與清淡日還沒測過，且本次又加派了央行與黃金的採集責任，先保留餘裕，觀察一週再考慮收回 11:30。

### 2026-08-04（第 9 次修訂 · 子類別 8 組擴為 15 組、新增四家來源）

**動機**：本知識庫已不只是每日閱讀中心，它同時是**每週主題匯流訊號報**與**每月 House View** 的上游資料庫。對照台新投顧全策組 2026/07/30 的 House View 簡報，它的章節是「能源／利率／美股／科技／信用／其他區域（歐、日、中）／黃金」加策略頁的區域展望——**而知識庫當時只有 8 組，中國、日本、歐洲全部擠在「台股與亞太」與「央行」裡，信用債、黃金、生技健護則根本沒有收納位置**。月底要寫那幾頁時撈不出連貫素材，等於每個月重做一次研究。

- **市場總經由 3 組擴為 7 組**：「台股與亞太」拆成 **台灣／中國／日本／亞太（韓國、印度、東南亞）**，並**新增歐洲**。歐洲是使用者原本沒提、但比對 House View 後補上的——它每月都有專頁與 Stoxx 600 目標價，卻是唯一沒有收納位置的區域。
- **產業與主題由 3 組擴為 6 組**：新增 **生技健護／信用債／黃金**。
- **新增第 4.1 節「分級下限」**：核心組 ≥10、次要組 ≥6、衛星組 ≥3，合計下限 91 則，全站目標 95～115 則。**一律 ≥10 會變成 150 則下限，做不到，而且會逼出注水內容。** 分級數字是拿 8/04 的實際素材校準的：那天全站 91 則，其中台灣本地 9、日本 7、中國 3、韓印東南亞 3、歐洲 3（且都只是順帶提及）、生技 3、黃金 1、**信用債 0**。衛星組若訂 ≥6，第一天就有五組達不到。
- **衛星組加「保底數據卡」機制**：信用債與黃金即使當天無新聞，也各要有 1 則數據卡（ICE BofA OAS 利差／金價與 WGC 流向）。**這不是湊數**——信用利差與 CSP 債務正是 House View 的核心論述（簡報 Slide 11），每天記一個數字，累積起來就是那條可回溯的曲線。衛星組的價值本來就在跨期累積，不在單日厚度。
- **來源由 20 家增為 24 家**：新增 **Fierce Biotech、STAT News**（生技健護，此前完全無專責來源）、**KED Global、Mint**（韓國、印度）。東南亞不另設來源，Nikkei 覆蓋已足夠。官方源新增 FRED 的 ICE BofA OAS、FDA、World Gold Council。
- **subagent 由 6 組擴為 7 組**，新增 G 組（生技＋亞太），並**在每組後面標註它負責餵飽哪些子類別**。8/04 的教訓很明確：信用債 0 則不是因為沒新聞，是因為**沒有人負責去找**。同理 A 組加了信用債 ≥3 的責任、F 組門檻由 8 提高到 10（台灣升格核心組，而 8/04 剛好只有 9 則）。
- **深度卡改為總量 6～8 張，不再是每組 1 張**。15 組每組一張會變成 15 張、撰寫量翻倍。四個核心組各 1 張為基本盤，其餘看當日主軸。
- **完成目標由 11:30 放寬為 12:30**。8/04 那輪 09:40 完稿、遠早於 11:30，代表原目標有餘裕；但這次增量不小，先放寬觀察一週再考慮收回。
- **檢查腳本新增 `QUOTA` 表與 `missing` 檢查**。舊版寫死 `>=10`，15 組分級後無法用單一數字判定。`missing` 那一行是防止**整組漏建**——只檢查「有出現的組夠不夠」會漏掉「這組根本沒建」，而那正是新增子類別後最常見的失敗模式。
- **新增歸類原則**（第 4.1 節末），處理拆組後最容易混淆的邊界：日圓／BOJ 歸日本組而非央行組（8/04 光日圓干預就有 5 則掛在央行組，不移走的話央行組會被抽空、日本組卻空著）、信用債只收利差與償債、黃金只收貴金屬、生技與金融的併購案不得兩邊各開一張卡。
- **新增退場機制**：衛星組若連續一週有三天以上湊不到 3 則，回頭檢討是否併回母組，而不是每天硬擠。**子類別存在的理由是下游用得到，不是版面好看。**
- **`index.html` 同步**：新增 `.b-fierce`／`.b-stat`／`.b-ked`／`.b-mint` 四個徽章 CSS、`BADGE` 對應、`SRCBAR` 由 20 擴為 24。分組本身是動態渲染，**新增子類別不需要改前端**。

### 2026-08-04（第 8 次修訂 · 新增 SemiAnalysis、釐清 Reuters 攔截的處置）

- **來源自 19 家擴充為 20 家**：新增 **SemiAnalysis**（`newsletter.semianalysis.com`，徽章／`src` 值＝`semi`），歸在付費訂閱那一組，併入 D 組（Reuters ＋ Tom's Hardware ＋ OGJ ＋ SemiAnalysis）。動機是本站「AI 與半導體」子類別的素材長期偏向股價與財報，缺少加速器架構、記憶體與 CoWoS 供應鏈層級的數字；SemiAnalysis 正好補這一塊，而且它的顆粒度是其他 19 家都沒有的。
- **新增第 1.2 節專講 SemiAnalysis 的特殊處理**。它是 Substack 電子報、**約每週 1～2 篇而非日更**，這件事必須寫死在規格裡：**大多數日子的 24 小時窗口內不會有新文章，那是正常狀態、不是採集失敗**，不必記為降級，更不可以為了讓它出現而收錄窗口外的舊文。窗口規則對所有來源一視同仁，沒有例外——這是本次最容易被後續執行者破壞的地方。另註明 Substack 頁尾永遠掛訂閱 CTA，**不可據此判定被擋**，一律回歸第 1.1 節的內文量標準（8/04 實測單篇 107 段、25,441 字元）。
- **`index.html` 同步**：新增 `.b-semi` 徽章 CSS、`BADGE` 對應（顯示為 SemiAnalysis），`SRCBAR` 由 19 擴為 20。這是少數需要動外殼的情況。
- **第 1.1 節新增「Reuters 的 DataDome 攔截屬間歇性」**。8/04 早上該站對所有頁面回傳 CAPTCHA 挑戰頁、約 10 次重試皆失敗，導致當日整版沒有任何 Reuters 卡片；**同日稍晚以同一台瀏覽器重測即完全恢復正常**。所以遇到就當次放棄並如實記錄（不得繞過，第 2 節紅線），但**不要把它移出來源清單或調降配額**，並補上 D 組的補位規則（Tom's 與 OGJ 各多讀 3–5 篇）。
- **檢查腳本的 `SRCOK` 白名單加入 `semi`**，`src` 未同步會讓前端渲染出沒有樣式的空徽章。
- **已知風險**：8/04 那一輪八個子類別中有三組（金融併購與企業、能源與原物料、地緣政治）**剛好卡在 10 則、沒有任何緩衝**，而那還是素材充足、且 Reuters 缺席的一天。若遇上週末或清淡日又同時掉一家主力來源，這三組很可能湊不齊。SemiAnalysis 對「AI 與半導體」有幫助，但那一組本來就最寬裕，**補不到真正吃緊的三組**。下次體檢時應優先考慮把這三組的下限由 10 調降為 8，而不是放寬窗口。

### 2026-08-03（第 7 次修訂 · 檢查腳本改用絕對路徑）

- **`data/` 的三處讀取改用 `REPO` 絕對路徑**（原為 `data/...` 相對路徑）。起因是維護作業當天為了寫 podcast 那條的文件，把 `~/podcast-knowledge-digest` 也連進了排程任務的工作資料夾。**兩個 repo 有 7 個同名檔案**：`AGENT_BRIEF.md`、`MAINTENANCE.md`、`README.md`、`index.html`、`data/index.json`，以及 `data/2026-07-30.json`、`data/2026-08-02.json`。相對路徑在錯的目錄下執行就會讀到另一個 repo 的同名檔。
- **這次的失效多半會吵而不會安靜**（podcast 的 `index.json` 的 `days` 沒有 `file` 鍵，會 `KeyError`），但不能靠這個僥倖——結構哪天改了就變成安靜失效。絕對路徑讓整類問題消失，成本是三行。
- **排程執行時只需要本 repo 的工作資料夾**，不要連 podcast 那個。維護作業若需要同時操作兩邊，做完記得移除。

### 2026-08-03（第 6 次修訂 · 讓檢查腳本自己不會安靜失效）

第 5 次修訂把 brief 與 prompt 的兩段程式碼對齊了，本次處理的是**腳本本身的健壯性與涵蓋範圍**，外加兩處過時的事實。

- **檢查腳本改為缺 `ts` 也不中斷（最重要的一項）**。舊版直接寫 `dt.datetime.fromisoformat(c['ts'])`，任何一張卡漏填 `ts` 就 `KeyError`，整支腳本掛掉——而執行者極可能因此跳過檢查直接發布。**檢查機制自己安靜失效，比沒有檢查更危險**，這與 `auto-push.sh` 那起事故是同一種模式。改為 `T(c)` 包 try／except 回 `None`，並獨立統計「缺/壞 ts」的張數。**8/4 是 `ts` 第一次上線，正是最容易觸發舊版這個缺陷的一天。**
- **把三項原本靠眼睛看的檢查寫進腳本**：`src` 白名單、`url` 是否為列表頁、全站總則數是否落在 80～95。8/3 那版就有一個列表頁 url（`bloomberg.com/markets/stocks`）被當成兩張卡的出處，而且同組——散文規則擋不住，腳本可以。
- **新增「未來 `ts`」檢查**（晚於 `window.to`）。時區換算錯會產生未來時間戳，窗口比對照樣跑得過，只是收了不該收的。
- **列表頁採啟發式判斷並標為警告而非硬性失敗**：最後一段網址沒有連字號、也沒有 4 位以上數字 ID 就標記。在 8/3 的 139 個真實網址上實測**零誤判**，只抓出那一個真正的列表頁；TWSE 的 `.html` 表單頁會被標出來屬預期，人工確認即可。
- **修正 `podcast-digest-daily` 的排程時間**（第 0 節、第 6 節）。兩處都還寫 09:00，實際自 8/3 起已改為 **03:00**；且「避免兩個任務搶 Chrome」這個錯開理由也已不成立——那條線改用 podfetch＋Gemini API 之後不再使用 Chrome。現行理由是分散 token 用量。
- **第 0 節補上指向 `MAINTENANCE.md` 第 6 節的電源設定交叉引用**。那台 Mac 需要從 00:55 連續醒著到中午，靠的是 `pmset -c sleep 0` 加排定喚醒，**這組設定不存在於任何設定檔中，重灌或換機不會遷移**——只讀 brief 的人原本完全看不到這件事。
- **修正第 5 節 `1.5.` 的清單編號**，改為第 1 點下的子項，否則 Markdown 會把後面的有序清單從 1 重新起算。

### 2026-08-03（第 5 次修訂 · 修補第 4 次修訂當天留下的破綻）

第 4 次修訂改動幅度大，當天就在 brief 與 prompt 之間留下三處新的不同步，另有一處事實寫錯。這幾項都是「照著跑會出事」等級，不是文字潤飾。

- **第 1.1 節的 JS 片段補上抓發布時間**。第 4 次修訂把 `ts` 訂為必填欄位，卻只在排程 prompt 裡更新了讀法，brief 第 1.1 節的 JS 仍然只抓內文段落。**照 brief 執行會拿不到 `ts`，而 `ts` 是整個 24 小時窗口制的地基。**
- **發布前檢查腳本同步為含拆卡判定的版本**。brief 留著舊版（只印本版內重複的總數），prompt 已是新版（逐一判定「至多 3 張、不得兩張同組」）。舊版看到數字大於 0 也不知道該不該放行。**檢查腳本是可執行的規格，它漂移的後果比散文漂移嚴重。**
- **移除第 5 節 E 組的「先做登入狀態測試」**。這個說法在第 2 次修訂就被推翻了（付費牆不能用登入狀態判斷），殘留在分組說明裡會誘導執行者退回舊做法。改為明確指向第 1.1 節的內文量標準。
- **修正「窗口不重疊」這個錯誤描述**（第 3 節、第 8 節）。原文寫「固定起點讓每天的窗口彼此接續、不重疊、不留縫」——**「不留縫」正確，「不重疊」是錯的**。相鄰兩天實際重疊約 4～5 小時（今日 07:00 至寫入時刻）。而且這個重疊有價值：重疊區間正是今天採集作業進行中的時段，那段時間發布的新聞今天很可能沒讀到，有重疊才能在明天補收，不會永久掉單；重複成卡由跨版去重擋住。寫成「不重疊」會讓人誤以為今早的新聞明天不該收，白白放棄補救。已補上時間軸圖示說明。
- **更新第 3.5 節的 `index.json` 範例**，原範例仍是 `count: 2`、`cards: 131`、`keptDates` 三個日期、無 `window`，照抄會寫出舊制格式。

### 2026-08-03（第 4 次修訂 · 時效改為滾動 24 小時窗口）

**動機**：自 8/02 改為封存制、歷史永久保留之後，「單日版本保留最近 3 個日期」這條規則就失去了原本的理由。它當初存在，是因為舊制只有一份會被覆寫的頁面，多留兩天是怕讀者錯過前天的重要新聞。現在讀者隨時能用日期切換列回看任何一天，再讓兩天前的舊卡佔住今天的版面，只會稀釋「今天」的資訊密度——8/3 那一版 139 則裡有 94 則是舊卡，佔 68%。

- **核心規則改為滾動 24 小時窗口**（第 3 節）。窗口 `from` ＝ **前一日台北 07:00（固定）**，`to` ＝ 寫入時刻，實際長度 24～30 小時。**起點刻意不用「產出時刻往前推整 24 小時」**：本站在台北上午產出，而前一晚美股收盤（美東前一日 16:00 ＝ 台北當日 04:00）的報導多半掛前一日日期，浮動起點會把當天最重要的素材整批切掉。（**更正**：本條原寫「每天的窗口彼此接續、不重疊也不留縫」，其中「不重疊」有誤，相鄰兩天實際重疊約 4～5 小時，且該重疊是刻意保留的容錯設計；已於第 5 次修訂更正，詳見第 3 節。）
- **卡片新增必填欄位 `ts`**（第 3.5 節），ISO 8601 含時區、**一律換算為台北時間**。沒有 `ts` 就無法程式化驗證窗口，只靠 `date` 欄（只有日期沒有時分）做不到 24 小時精度。`date` 欄保留且必須與 `ts` 的台北日期一致——**前端只讀 `date`，完全沒用到 `ts` 與 `keptDates`，所以這次 schema 變更不需要動 `index.html`，舊檔也完全相容。**
- **頂層新增 `window` 欄**記錄本次窗口起訖；`keptDates` 語意變成「窗口涵蓋到的日期」（通常兩個），保留純為向下相容。
- **「當日新卡佔全站 1/4～1/3」作廢**，改為全站卡片 100% 為當日新寫，目標 80～95 則。**明文禁止從前一版複製或改寫任何卡片。**
- **深度卡由每子類別 2 張降為 1 張、中長摘要由 300～400 字降為 250～350 字**（第 4 節）。這是為了讓 80～95 則的撰寫量能在 11:30 前完成而做的取捨——寧可 8 張紮實的深度卡，不要 16 張注水的。
- **降級順序重寫**（第 5 節），並新增一條硬規定：某子類別湊不到 10 則時，先用「前瞻／最新一次」框架補，仍不足就**如實記錄在 `run` 欄並照實發布，絕不回頭撈舊卡湊數**。這是本次改版最容易被悄悄破壞的地方——撈舊卡沒有人會發現，但整條規則就此失效。
- **發布前檢查改寫**：日期直方圖那條換成「所有 `ts` ≥ `window.from`」與「`date` 與 `ts` 日期一致」，去重比對範圍由「本版內部」擴大到「本版 ＋ 前一版」，並附上可直接執行的檢查腳本。腳本取前一版時**刻意不用 `days[1]`**，改為「`days` 裡日期不等於今天的最新一筆」——因為 `days[1]` 只在「`index.json` 已先更新」時才正確，順序一顛倒就會拿去跟前天比對，去重照樣跑得過但實際失效。
- **新增「彙整型文章拆卡規則」與「`url` 不得為列表頁」**（第 5 節第 7 點）。體檢 8/2、8/3 兩版時發現各有 6 組卡片共用同一連結：一部分是週行事曆彙整拆成三個子類別（合理，明文允許，上限 3 張且不得同組），另一部分是 `bloomberg.com/markets/stocks` 這種即時指數頁被當原文連結（不合理，該頁隔天內容就變了，讀者點進去看不到卡片描述的內容）。不先把這兩種情況區分清楚，跨版去重上線後會直接卡住合理的拆卡。
- **已知風險**：這是本站上線以來最大的一次規則收緊，8/4 那一輪是第一次實測。要特別看兩件事——(1) 八個子類別是否真的都能在 24 小時窗口內湊到 10 則；(2) 產出是否還能在 11:30 前完成。任一項失守就要回頭調整，調整方向應該是**先降 `≥10 則` 的下限，而不是放寬窗口**。

### 2026-08-03（第 3 次修訂 · 修補 brief 與排程 prompt 的四處不同步）

本次沒有新增來源或改版面，純粹是把兩份規格對回同一套事實。第 2 次修訂當天改動很急（邊產出邊修正付費牆誤判），只改了排程 prompt 而漏掉 brief，隔天體檢才發現落差。

- **子類別定義改為固定八組**（第 4 節）。brief 原本沿用最早的分法，產業分頁寫「AI／科技／金融／生技／原物料」共 10 組，但排程 prompt 與 8/2、8/3 的實際產出都是八組。**以八組為準**，並明寫生技併入「金融、併購與企業」、原物料併入「能源與原物料」，避免下一次有人照 brief 去補開兩個空分組、反而觸發 `≥10 則` 下限做不到。
- **降級順序移除「先砍 Barron's 與 IBD」那一階**（第 5 節）。那條規則的前提是這兩家常被擋、試了也白試，而第 1.1 節已推翻此前提。留著它等於把當天最該讀的美股選股視角排在第一個被犧牲。省時間改從深度卡張數下手。
- **每日新卡量口徑統一為 35～45 則**（第 3 節）。brief 寫 35～40、prompt 寫 35～45，8/3 實際 45 則——照 brief 會被判超標，照 prompt 才合格。取較寬的口徑。
- **瀏覽器 fallback 規則改為排除法**（第 6 節）。原本寫「HOME 不在清單時改選 `connectedAt` 最大的一台」，但實測 WORK 的 `connectedAt` 比 HOME 大，這條 fallback 會穩定選到已停用的辦公室機器——是個會沉默失敗的錯誤。改為明確排除 WORK 的 deviceId；只剩 WORK 時視同無瀏覽器可用，走免費來源降級。
- **F 組（台股專組）加上最低產出要求與 TWSE／MOPS 具體抓取路徑**（第 5 節）。8/3 F 組回傳 0 則，「台股與亞太」18 則裡沒有一則來自台灣媒體，等於新增三家來源完全沒發生效果。現規定鉅亨 ≥3、MoneyDJ ≥3、合計 ≥8 則，並寫死四個官方頁面網址與「連兩次抓不到就改用媒體轉引並註明」的退路，避免 F 組安靜地空手而回。

### 2026-08-03（第 2 次修訂 · 讀取方法與瀏覽器綁定）

- **修正付費牆判斷方式（重要）**：新增第 1.1 節。原本以「導覽列是否出現 Sign In」判斷來源可否讀取，**這是錯的**——Bloomberg 與 Barron's 的導覽列永遠顯示 Sign In／Subscribe Now，但內文照常完整載入。改為以實際取得的內文量判斷（段落數 ≥8 且 ≥1,500 字視為完整）。
- **改變主要讀法**：`get_page_text` 在 Bloomberg、Barron's、MarketWatch 上會嚴重低估內文，僅適合掃標題。正式讀取改用 `javascript_tool` 抓 `article p`，並先等 3–5 秒讓頁面渲染。
- **綁定瀏覽器 deviceId**：第 6 節新增 HOME／WORK 對應表。`list_connected_browsers` 回傳的 `name` 不反映使用者命名、`isLocal` 兩台皆為 true，無法分辨，故直接寫死 HOME 的 deviceId。
- **影響**：8/3 當日版本因此誤判而未收錄任何 Barron's 卡片，Bloomberg 亦僅採用部分段落；該日 `about.run` 已加註更正說明。

### 2026-08-03（第 1 次修訂 · 來源擴充）

- **來源自 15 家擴充為 19 家**：Reuters 由「公開／官方補充」升格為具名來源（徽章 `reuters`，不再併入 `pub`）；新增鉅亨網（`anue`）、MoneyDJ（`moneydj`）、TWSE 與公開資訊觀測站（`twse`）。動機是台灣本地內容過薄——8/3 當天「台股與亞太」子類別沒有任何一則來自台灣媒體。
- **`index.html` 同步**：新增四個徽章 CSS 與 `BADGE` 對應，`SRCBAR` 由 15 擴為 19。這是少數需要動外殼的情況。
- **subagent 分組改為 6 組**（原 5 組），新增台股專組（F）與 Reuters 組（D）。
- **新增每日必抓的官方數據**：台股三大法人買賣超與融資餘額、CME FedWatch 機率、EIA 週報庫存。動機是先前在盯盤時程列出「融資餘額」當追蹤指標，卻沒有對應的資料來源。
- **新增兩條發布前檢查**：當日新卡不得與既有卡片共用同一原文連結（8/3 首次建檔時抓到 9 張重複）；每張卡片的 `src` 必須在徽章清單內。
- **新增時間預算與降級順序**：目標 11:30 前完成，落後時的取捨有明確優先序。

### 2026-08-02

- 改為封存制：`index.html` 拆成外殼＋`data/*.json`，每日獨立存檔永久保留，頁面最上方加日期切換列（第 3.5 節）。
