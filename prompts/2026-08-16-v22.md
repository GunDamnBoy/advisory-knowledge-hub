# 排程 prompt 快照 · v22（2026-08-16）

> **這是 `advisory-dashboard-daily` 排程任務 prompt 的存檔副本，不是執行用的檔案。**
> 正本在 `~/Documents/Claude/Scheduled/advisory-dashboard-daily/SKILL.md`（不在本 repo、不在 git）。
>
> **v22 相對 v21 的差異**（第 22 次修訂）：
> ① 卡片字數**追認為一般卡 650–800、深度卡 900–1,200**，`check.py` 開始印字數統計（只警告不擋）；② 坑由五條擴為六條，新增**讀新聞一律 navigate、不要用 fetch**；③ 新增**「窗口外損失」要先查前一版**的判定規則；④ 法說會前瞻改走 **MOPS 法人說明會一覽表**、GLD **日變化讀前一日封存自己相減**；⑤ 降級門檻與回報項目同步。

---

```
---
name: advisory-dashboard-daily
description: 每天 07:30（一週七天）讀 25 家財經／政治／台股／生技／亞太來源，產出投顧知識庫儀表板當日封存（15 個子類別）並發布到 GitHub Pages
---

你要執行「投顧知識庫儀表板」的每日產出。全程繁體中文（台灣用語），讀者為投顧從業人員。

**本任務只需要 `~/advisory-knowledge-hub` 這一個工作資料夾**；若還掛著 `~/podcast-knowledge-digest`（另一條產線，兩 repo 有 7 個同名檔案）**完全不要碰**，讀寫一律用絕對路徑。

## 第 0 步：讀規格與工具（一定要做）

1. 用 Read **完整讀** `/Users/kenny/advisory-knowledge-hub/AGENT_BRIEF.md`——它是唯一完整規格，本 prompt 只是流程摘要，**細節與衝突一律以 brief 為準**。讀不到＝資料夾未連線，停下回報「repo 資料夾未連線，今日無法產出」。**不需要讀 CHANGELOG.md**（純歷史）。
2. 用 Read 讀這三份（稍後要貼進 subagent 提示）：
   - `scripts/subagent_preamble.md`（採集員共用前言）
   - `scripts/list_timestamps.js`（列表頁預篩）
   - `scripts/read_article.js`（讀正文）

## 第 1 步：日期與窗口

1. `TZ=Asia/Taipei date "+%Y-%m-%d %H:%M %A"`。
2. 窗口：`from` ＝ **前一日台北 07:00（固定）**、`to` ＝ 預計寫入時刻；只收 `from` 之後發布的新聞（細節見 brief 第 3 節；相鄰兩天刻意重疊 2～5 小時屬正常，那是接住邊界素材的容錯）。
3. **判斷今天是不是週末模式**：`window.from` 那一天若為週六或週日 → 套用 brief 第 4.1.1 節的**週末下限**（次要組 ≥4、衛星組 ≥2、全站 80～125；核心四組不變）。實務上是**週日與週一這兩輪**。`check.py` 會自動判定並在輸出標明模式，但**派工前你就要知道**——這決定要不要派人去掃週末不發稿的來源。
4. 讀 `data/index.json` 看 `days[0]`：已是今天＝重跑（覆蓋今天的檔）。**絕不刪改任何既有 `data/YYYY-MM-DD.json`。** 不需要看昨天的日檔內容，去重由 check.py 代勞。**但 index.json 是跨日記憶，要從前幾天的 entry 記下三樣東西**：`threads`（今天掛 thread 沿用同一代號）、昨天的 `watch`（今天要逐條寫「昨日節點回顧」）、昨天的 `pulse`（判斷五資產立場有無翻轉）。

## 第 2 步：選瀏覽器（開 subagent 之前）

排程情境**不要呼叫 AskUserQuestion**。直接 `select_browser("8f82131f-7af7-4a5d-a5d7-93677f4e3884")`（HOME）；fallback 與可用性測試依 brief 第 6 節（**排除 WORK `120b7860-…`，不要用 connectedAt 判斷**）。都不行→只用免費來源產出並在 `run` 欄明寫，**寧可誠實降級也不要開天窗**。subagent 回報「Multiple Chrome browsers」→叫它**重選 HOME**後重試，不要中止；**絕不選 WORK**（8/13 F 組誤選過，選錯不會報錯）。

## 第 3 步：採集（25 家，7 組**分兩批**派工）

**⚠ 不要七組同時開。** Chrome 分頁群組容量只有約 4～6 格：

- **第一批（4 組）：A、C、E、F** ——核心配額與市場數據優先落地。
- **第二批（3 組）：B、D、G** ——**等第一批全部回報後**再派。

**每個 subagent 的提示 ＝ `subagent_preamble.md`（**第 3.5 節眉角表只留該組會用到的那幾列，其餘刪掉**；其他節照貼）＋ `list_timestamps.js` 全文 ＋ `read_article.js` 全文 ＋ `window.from` 的值 ＋ 是否週末模式 ＋ 該組的來源清單與配額。** 讀法、分頁所有權、熔斷、發稿日曆、黑名單、探針策略、合規紅線、回報格式**全部已在前言**，不要在提示裡重寫。

**⚠ 六條會浪費整組配額的坑**（細節在前言第 2.5／3.5／3.6 節）：
- **每組開場 `tabs_create_mcp` 建自己的分頁、記住 `tabId`、每次呼叫都帶它、收工前關掉。絕不用 `tabs_context_mcp` 的第一個分頁當工作分頁**——那是共用的，各組會互相把對方的頁面導走（8/13 因此回合數 805→1,785；8/14 改正後降到 432、被導走 0 次）
- **用 `browser_batch` 把「導航＋等待＋讀取」合併成一次呼叫**，拆開的每個空隙都是別組插進來的機會
- **讀新聞一律 `navigate`＋`read_article.js`，不要用 `fetch`**——跨網域對新聞站必然失敗（8/16 G 組對 `biz.heraldcorp` 用 fetch，連撞兩次同篇上限，改 navigate 立刻成功）。**那是用錯工具，不是被擋**
- **三條上限**：同一種工具連續失敗 5 次熔斷、**同一篇文章失敗 3 次就換篇**、同一來源文章頁 15 篇收工。**工具失效不等於被擋，回報時要分清楚**
- **預篩一律從列表頁做**；取不到時間戳先用探針，且**單一來源最多掃 2 個分區**
- **週末模式時，OGJ／Fierce／FDA／IBD／TrendForce／SemiAnalysis 直接跳過**

分組（括號＝負責子類別與最低配額，**週末依 4.1.1 節放寬**）：
- A：Bloomberg＋WSJ（美股與財報、**信用債 ≥3**、**央行政策面 ≥5**）
- B：FT＋NYT＋WaPo＋The Economist（**歐洲 ≥3**、美國政治與政策、金融併購與企業）
- C：Nikkei＋華爾街見聞＋CNBC＋MarketWatch（**日本**、**中國**、**央行市場數據面 ≥3**、**全套市場數據**）
- D：Tom's Hardware＋OGJ＋SemiAnalysis＋TrendForce（能源與原物料、**黃金 ≥3**、AI 與半導體）
- E：Politico＋The Hill＋Barron's＋IBD（地緣政治、美國政治與政策、美股選股）
- F：鉅亨網＋MoneyDJ＋TWSE/TPEx 官方 API（**台灣 ≥10**；鉅亨 ≥4、MoneyDJ ≥3）
- G：Fierce Biotech＋STAT＋Korea Herald＋Mint（**生技健護 ≥3**、**亞太 ≥3**）

**派工前核對分派表涵蓋全部十五組**（歷史教訓：漏了央行組就是 4 則開天窗）。央行組取材面要寬（各國央行／公債標售／曲線與利差／衍生品定價／匯率政策／財政交界，見 brief 第 5 節）。**Reuters 與 KED 已移除，讀到也不要收。**

**補位上限 2 輪**（8/10 跑了四輪、佔全輪成本 22.6%）。**派補位工時把「已收 URL 清單」放進提示**。兩輪後仍有缺額就接受，在 `run` 欄具名記錄。

**必抓數據**（完整清單與台股官方端點見 brief 第 5 節第 3 點）：市場全套報價、台股官方端點（**法說會前瞻改走 MOPS「法人說明會一覽表」**——OpenAPI 第 12 款是「當日重大訊息」、週末回傳 0 筆）、**升降息機率（主源 Investing.com Fed Rate Monitor**，CME FedWatch 已是跨網域 iframe＋reCAPTCHA，不要再每天先試 CME）、**信用債保底卡**（FRED 兩條 OAS）、**黃金保底卡**（SPDR **GLD＋GLDM 持倉＋ETF 折溢價**；**COMEX 庫存已停止追蹤**；**持倉日變化讀前一日的 `data/*.json` 自己相減，官網沒有歷史 API**）。**報價一律標時間戳與口徑。**
**⚠ 台股官方端點：先 `navigate` 到與端點「同網域」的頁面，再用 `javascript_tool` 跑 `await fetch(url).then(r=>r.text())`。** 不要 navigate 到 JSON 網址（Chrome 會下載不會渲染）；**跨子網域會被 CORS 擋**——`www.twse.com.tw`／`openapi.twse.com.tw`／`mopsfin.twse.com.tw`／`www.tpex.org.tw` 各自對應，換網域要重新 navigate。

## 第 4 步：寫檔

依 brief 第 3.5 節 schema 產生 `data/<今天>.json`，**隨即更新 `data/index.json`**（加進 `days`、新到舊排序、`count`＝長度、**當日 entry 必填五個跨日記憶欄位：`thermo`（整數）、`threads`（陣列）、`watch`（當日 watch 的 d/t 鏡像）、`pulse`（當日 pulse 的 k/dir 鏡像）、`snap`（當日 snap 的 k/num/chgPct 鏡像）**）。

核心規則（細節見 brief 第 3、3.5、4、4.1、4.1.1 節）：
- **100% 窗口內發布且當天新寫**，禁止複製前一版任何卡片。平日全站 95～125 則、**週末 80～125 則**。
- 頂層 `window`；每卡必填 `ts`（ISO 8601、台北 +08:00）與 `date`（與 ts 日期一致）。
- **v17 時間序列欄位（check.py 會擋）**：`overview.thermo.level` 一律 **0–100 整數字串**（質性描述放 `note`）；`snap` 每格附 **`num`／`chgPct` 數值欄**（不適用時 `chgPct` 給 `null`）；跨日延續的題材在卡片加 **`thread`** 代號（一天 3～6 張為宜，**沿用 index.json 前幾天的既有代號**）。
- **v18 判斷欄位（check.py 會擋）**：`overview.pulse` ＝ 五資產立場列，固定五鍵順序 **美股／美債／美元／黃金／原油**、`dir` 只能是 偏多/中性/偏空、`note` ≤30 字講理由——**翻轉必須有意識且 note 講清楚為什麼**；`overview.watchReview` ＝ 把 index.json 昨天 entry 的 `watch` 逐條回顧，`verdict` 只能是 應驗/落空/未決＋一句話 `note` 附數字——**判定要誠實，模稜兩可就是未決，落空的 note 最有價值**。
- **`about.run` 第一句寫「本輪由 <模型名稱> 執行」。模型名稱取自工作階段 metadata，不要憑對話中的自我描述填**（8/14 首日就寫錯成「Opus 4.5」、實際是 `claude-opus-5`）。**取不到就寫「未確認」——寫錯的版本號比空著更糟。**
- **要把某則寫成「窗口外遭排除的損失」之前，先確認前一版的封存裡沒有它。** 相鄰窗口刻意重疊 2～5 小時就是為了接住邊界素材——8/16 曾把兩則其實已在 8/15 收錄的素材寫成損失，**容錯成功被寫成失敗**。
- 十五組分級下限見 4.1 節，**週末依 4.1.1 節放寬**。**分組 `label` 逐字依 brief 第 4.1 節**。歸類原則（日圓→日本組等）同節。
- **深度卡 6～8 張（約 900～1,200 字）；其餘中長摘要 650～800 字**＋2～3 bullet（2026/08/16 追認實際產出水準）。`check.py` 會印出字數統計但**只警告不擋**——**不要為了湊字數注水，那比超標更糟**。三分鐘總覽、約 1,000 字心得、盯盤時程、`stamp` 全部重寫。
- `url` 用單篇永久連結；跨版與版內去重及豁免依 brief（豁免清單在 check.py 的 `DEDUP_EXEMPT`）。彙整型文章至多拆 3 張、不得同組。
- `about.access` 只寫來源構成、不寫合規聲明；時效說明寫本次窗口起訖；來源家數寫 25 家。
- 素材不足用「前瞻／最新一次」框架補（`ts` 掛分析發布時間）；**絕不放舊聞、不編造、不回撈前一版湊數**。週末清淡日照常出。
- **姊妹庫匯流條與異常旗標是純前端功能，writer 不用產任何對應資料。**

## 第 5 步：檢查與發布

1. **絕不跑任何 git 指令**（含 `git status`）——本機 dashpush 每 180 秒自動推送。
2. 跑 `python3 /Users/kenny/advisory-knowledge-hub/scripts/check.py <今天>`，**出口碼必須為 0**。它會自動判定平日／週末模式、檢查 v17/v18 欄位（thermo 整數、snap.num、pulse 五鍵、watchReview 完整性、index 五個跨日記憶欄位、thread 格式），並印出卡片字數統計（字數只警告、不影響出口碼）。有失敗就修到 0 再發，「疑似列表頁」是警告、人工確認即可。
3. 等 2～4 分鐘後驗證線上 `https://gundamnboy.github.io/advisory-knowledge-hub/data/index.json` 的 `days[0].date` 是今天（WebFetch 會吃到 CDN 快取，用 Chrome `fetch(url+'?cb='+Date.now(),{cache:'no-store'})`）。

**時間預算**：12:30 前完成（實測 09:25～12:05；分頁所有權上線後多在 09:45 前）。落後時依序砍：深度卡→4 張、**中長摘要 650～800 字降為 350～450 字**、次要與衛星組篇數減三分之一（**核心組不減**）。**不可砍**：窗口、分級下限、市場數據、台股官方數據、保底卡、`run` 欄誠實記錄、**v17/v18 欄位**。**中途因用量上限等原因中斷**：確認未寫入檔案後整輪重跑，窗口 `to` 用重跑當下時刻。

## 第 6 步：回報

簡短回報：四條主軸、十五組則數（標出未達下限者）、全站總數、窗口起訖、**平日或週末模式**、**當日 thermo 值、五資產立場（有無翻轉）、節點回顧戰績（N 應驗／N 落空／N 未決）、掛了哪些 thread**、網站連結、實際完成時間。**主動講**：**本輪由哪個模型執行（取自工作階段 metadata）**、check.py 的結果（**含字數統計那兩行**）、被擋的來源（依內文量實測，附段落數／字數）、**有沒有觸發任何來源的風控**、**分頁狀況（各組被導走幾次、有沒有撞到三條上限）**、擴充套件注入偵測、保底卡與台股端點取得情形、**補位跑了幾輪**、有無中斷重跑。**若某來源在窗口內零新文，講一句就好、不必當降級**（週末本來就這樣）。有降級就明說，不要粉飾。
```
