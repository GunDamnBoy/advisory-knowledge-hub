# 排程 prompt 快照 · v15（2026-08-09）

> **這是 `advisory-dashboard-daily` 排程任務 prompt 的存檔副本，不是執行用的檔案。**
> 正本在 `~/Documents/Claude/Scheduled/advisory-dashboard-daily/SKILL.md`（不在本 repo、不在 git）。
> 每次改完 prompt 都要在這裡存一份新的——`update_scheduled_task` 是整份取代，不存就永遠消失。
>
> **v15 相對 v14 的差異**：第 0 步改讀三份腳本（新增 `subagent_preamble.md`、`list_timestamps.js`）；
> 第 3 步的派工提示改為「共用前言全文＋兩支腳本全文＋差異部分」，不再逐份重寫讀法與合規；
> 新增列表頁預篩的強制規定與單一來源 15 篇上限；補位派工要帶「已收 URL 清單」；
> 回報項目新增「有沒有觸發任何來源的風控」。各來源眉角表已移入 brief 第 1.1.2 節。

---

（正本全文見排程任務；本檔以差異摘要為主，完整內容與 v14 的共同段落請對照 `prompts/2026-08-08-v14.md`。）
