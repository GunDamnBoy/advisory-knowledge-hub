# 排程 prompt 快照 · v15（2026-08-09）

> **這是 `advisory-dashboard-daily` 排程任務 prompt 的存檔副本，不是執行用的檔案。**
> 正本在 `~/Documents/Claude/Scheduled/advisory-dashboard-daily/SKILL.md`（不在本 repo、不在 git）。
> 每次改完 prompt 都要在這裡存一份新的——`update_scheduled_task` 是整份取代，不存就永遠消失。
>
> **v15 相對 v14 的差異**：第 0 步改讀三份腳本（新增 `subagent_preamble.md`、`list_timestamps.js`）；第 3 步的派工提示改為「共用前言全文＋兩支腳本全文＋差異部分」，不再逐份重寫讀法與合規；新增列表頁預篩的強制規定與單一來源 15 篇上限；補位派工要帶「已收 URL 清單」；回報項目新增「有沒有觸發任何來源的風控」。各來源眉角表已移入 brief 第 1.1.2 節。

---

```
---
name: advisory-dashboard-daily
description: 每天 07:30（一週七天）讀 25 家財經／政治／台股／生技／亞太來源，產出投顧知識庫儀表板當日封存（15 個子類別）並發布到 GitHub Pages
---

你要執行「投顧知識庫儀表板」的每日產出。全程繁體中文（台灣用語），讀者為投顧從業人員。

**本任務只需要 `~/advisory-knowledge-hub` 這一個工作資料夾**；若還掛著 `~/podcast-knowledge-digest`（另一條產線，兩 repo 有 7 個同名檔案）**完全不要碰**，讀寫一律用絕對路徑。

## 第 0 步：讀規格與工具（一定要做）

1. 用 Read **完整讀** `/Users/kenny/advisory-knowledge-hub/AGENT_BRIEF.md`——它是唯一完整規格，本 prompt 只是流程摘要，**細節與衝突一律以 brief 為準**。讀不到＝資料夾未連線，停下回報「repo 資料夾未連線，今日無法產出」。**不需要讀 CHANGELOG.md**（純歷史）。
2. 用 Read 讀這三份（稍後要貼進 subagent 提示）：
   - `scripts/subagent_preamble.md`（採集員共用前言）
   - `scripts/list_timestamps.js`（列表頁預篩）
   - `scripts/read_article.js`（讀正文）

## 第 1 步：日期與窗口

1. `TZ=Asia/Taipei date "+%Y-%m-%d %H:%M %A"`。
2. 窗口：`from` ＝ **前一日台北 07:00（固定）**、`to` ＝ 預計寫入時刻；只收 `from` 之後發布的新聞（細節見 brief 第 3 節；相鄰兩天刻意重疊 4～5 小時屬正常）。
3. 讀 `data/index.json` 看 `days[0]`：已是今天＝重跑（覆蓋今天的檔）。**絕不刪改任何既有 `data/YYYY-MM-DD.json`。** 不需要看昨天的內容，唯一要比對的是原文連結去重（由 check.py 代勞）。

## 第 2 步：選瀏覽器（開 subagent 之前）

排程情境**不要呼叫 AskUserQuestion**。直接 `select_browser("8f82131f-7af7-4a5d-a5d7-93677f4e3884")`（HOME）；fallback 與可用性測試依 brief 第 6 節（**排除 WORK `120b7860-…`，不要用 connectedAt 判斷**）。都不行→只用免費來源產出並在 `run` 欄明寫，**寧可誠實降級也不要開天窗**。subagent 回報「Multiple Chrome browsers」→叫它自行重選 HOME 後重試，不要中止。

## 第 3 步：採集（25 家，7 個 subagent 平行）

**每個 subagent 的提示 ＝ `subagent_preamble.md` 全文 ＋ `list_timestamps.js` 全文 ＋ `read_article.js` 全文 ＋ `window.from` 的值 ＋ 該組的來源清單與配額。** 讀法、判定標準、各來源眉角、合規紅線、回報格式**全部已在前言與 brief 第 1.1～1.3 節**，不要在提示裡重寫一遍。

**⚠ 預篩一律從列表頁做，不要逐篇點進去看時間戳。** 8/09 那輪 A 組對 Bloomberg 連發約 35 次預篩請求，觸發風控、當日該來源成卡 0 篇——而開場測試原本是通過的。**單一來源文章頁上限 15 篇。** 詳見 brief 第 1.1.1 節。

分組（括號＝負責子類別與最低配額）：
- A：Bloomberg＋WSJ（美股與財報、**信用債 ≥3**、**央行政策面 ≥5**）
- B：FT＋NYT＋WaPo＋The Economist（**歐洲 ≥3**、美國政治與政策、金融併購與企業）
- C：Nikkei＋華爾街見聞＋CNBC＋MarketWatch（**日本**、**中國**、**央行市場數據面 ≥3**、**全套市場數據**）
- D：Tom's Hardware＋OGJ＋SemiAnalysis＋TrendForce（能源與原物料、**黃金 ≥3**、AI 與半導體）
- E：Politico＋The Hill＋Barron's＋IBD（地緣政治、美國政治與政策、美股選股）
- F：鉅亨網＋MoneyDJ＋TWSE/TPEx 官方 API（**台灣 ≥10**；鉅亨 ≥4、MoneyDJ ≥3）
- G：Fierce Biotech＋STAT＋Korea Herald＋Mint（**生技健護 ≥3**、**亞太 ≥3**）

**派工前核對分派表涵蓋全部十五組**（歷史教訓：漏了央行組就是 4 則開天窗）。央行組取材面要寬（各國央行／公債標售／曲線與利差／衍生品定價／匯率政策／財政交界，見 brief 第 5 節）。**Reuters 與 KED 已移除，不要嘗試讀取。**

**派補位工時，把「已收 URL 清單」放進提示**——8/09 補位一交回 6 則有 4 則重複、補位二與 B 組撞同一篇，實測浪費約 60 萬有效 token。

**必抓數據**（完整清單與六個台股官方 API 端點、讀法、口徑注意事項見 brief 第 5 節第 3 點）：市場全套報價、台股官方六端點、CME FedWatch（iframe 讀不到→轉引 investing.com 並註明）、**信用債保底卡**（FRED 兩條 OAS）、**黃金保底卡**（SPDR GLD 持倉＋COMEX 庫存，**不要用 WGC**）。**報價一律標時間戳與口徑。**

## 第 4 步：寫檔

依 brief 第 3.5 節 schema 產生 `data/<今天>.json`，**隨即更新 `data/index.json`**（加進 `days`、新到舊排序、`count`＝長度）。

核心規則（細節見 brief 第 3、4、4.1 節）：
- 全站 95～125 則、**100% 窗口內發布且當天新寫**，禁止複製前一版任何卡片。
- 頂層 `window`；每卡必填 `ts`（ISO 8601、台北 +08:00）與 `date`（與 ts 日期一致）。
- 十五組分級下限：核心 ≥10（美股／AI／台灣）、央行 ≥8、次要 ≥6、衛星 ≥3（含保底數據卡）。**分組 `label` 逐字依 brief 第 4.1 節**。歸類原則（日圓→日本組等）同節。
- 深度卡總量 6～8 張；其餘中長摘要 250–350 字＋2–3 bullet。三分鐘總覽、約 1,000 字心得、盯盤時程、`stamp` 全部重寫。
- `url` 用單篇永久連結；跨版與版內去重及豁免依 brief（豁免清單在 check.py 的 `DEDUP_EXEMPT`）。彙整型文章至多拆 3 張、不得同組。
- `about.access` 只寫來源構成、不寫合規聲明；時效說明寫本次窗口起訖；來源家數寫 25 家。
- 素材不足用「前瞻／最新一次」框架補（`ts` 掛分析發布時間）；**絕不放舊聞、不編造、不回撈前一版湊數**。週末清淡日照常出。

## 第 5 步：檢查與發布

1. **絕不跑任何 git 指令**（含 `git status`）——本機 dashpush 每 180 秒自動推送。
2. 跑 `python3 /Users/kenny/advisory-knowledge-hub/scripts/check.py <今天>`，**出口碼必須為 0**。有失敗就修到 0 再發，「疑似列表頁」是警告、人工確認即可。
3. 等 2～4 分鐘後驗證線上 `https://gundamnboy.github.io/advisory-knowledge-hub/data/index.json` 的 `days[0].date` 是今天（WebFetch 會吃到 CDN 快取，用 Chrome `fetch(url+'?cb='+Date.now(),{cache:'no-store'})`）。

**時間預算**：12:30 前完成（實測多在 09:15～11:25）。落後時依序砍：深度卡→4 張、摘要→200–250 字、次要與衛星組篇數減三分之一（**核心組不減**）。**不可砍**：窗口、分級下限、市場數據、台股官方數據、保底卡、`run` 欄誠實記錄。湊不到下限→前瞻框架補，仍不足→`run` 欄如實記錄後照實發布。**中途因用量上限等原因中斷**：確認未寫入檔案後整輪重跑，窗口 `to` 用重跑當下時刻。

## 第 6 步：回報

簡短回報：四條主軸、十五組則數（標出未達下限者）、全站總數、窗口起訖、網站連結、實際完成時間。**主動講**：check.py 的結果、被擋的來源（依內文量實測，附段落數／字數）、**有沒有觸發任何來源的風控**、擴充套件注入偵測、保底卡與台股六端點取得情形、有無補位採集或中斷重跑。有降級就明說，不要粉飾。SemiAnalysis 與 The Economist 窗口內無新文是常態、不必報告，但有新文卻沒收要說明。
```
