# 待套用施工單 · 2026/08/18 判讀後執行

**這是草稿，尚未套用。** 2026/08/17 稽核（依 `writing-for-agents` 六槓桿）盤出的 11 項改動，全部動到排程每天會讀的檔（`AGENT_BRIEF.md`／排程 prompt／`scripts/`），因此一律排在 8/18 產出與判讀完成之後。

**執行前必做**：先跑 `/maintain` 第 1～3 步取當下現況（這份施工單的行號以 2026/08/17 的檔案狀態為準，brief 若已被別的對話改過，行號會偏移——**一律用引文錨點比對，不要信行號**）。

**套用完刪掉本檔。** 它本身就是這次稽核在抓的那種東西：一份會過期的抄本。

---

## 執行順序與批次

| 批次 | 項目 | 為什麼這樣排 |
|---|---|---|
| **第一批** | 1・2・3 | 抄本清除。低風險、改說明不改行為，且 `check.py` 與 grep 可驗。 |
| **第二批** | 4・5 | 沿革下沉試水溫。動的是敘事不是規則，驗次日產出無退化。 |
| **第三批** | 9・10・11 | 語言層。跟第二批一起搭比較省事（都在改散文）。 |
| **第四批** | 6・7・8 | 大手術。動到採集流程，等前三批確認無退化再做。 |

**一批做完就跑一次完整驗證清單（見本檔最後），並在 `CHANGELOG.md` 記一筆。不要連做兩批才驗。**

---

## 第一批

### 項目 1 · 成卡下限三份副本收成一份

**問題**：brief 內同一組數字寫了三次 —— §4.1 分級表、§4.1.1 週末表、§5 採集配額表的「下限」欄。權威在 `check.py` 的 `QUOTA`／`WEEKEND_QUOTA`。今日三份一致，但這正是 8/17 字數 bug 的形狀（一個規格有幾個數字，執行端就有機會挑錯一個），只是還沒炸。

**做法**：§4.1 與 §4.1.1 的兩張表**合併成一張（平日／週末並列）**，成為 brief 內唯一一份；§5 配額表**刪掉「下限」欄，只留配額**。

#### 1a. §4.1 換表

錨點（`### 4.1 分級下限` 之後那張表，`| 級別 | 子類別 | 下限 |` 起算三列）→ 換成：

```markdown
| 級別 | 子類別 | 平日 | 週末 |
|---|---|---|---|
| **核心**（每天都有大量素材） | 美股與財報、AI 與半導體、台灣 | **≥10** | ≥10 |
| **核心（特調）** | 央行、利率與匯率 | **≥8**（見下方說明） | ≥8 |
| **次要** | 中國、日本、能源與原物料、金融併購與企業、地緣政治（中東與戰事）、美國政治與政策 | **≥6** | **≥4** |
| **衛星**（低頻，重跨期累積） | 歐洲、亞太（韓印東南亞）、生技健護、信用債、黃金 | **≥3**，其中至少 1 則為保底數據卡 | **≥2** |
| **全站則數** | — | **95～125** | **80～125** |

**這是 brief 內唯一一份下限數字。** 權威值在 `check.py` 的 `QUOTA`／`WEEKEND_QUOTA`／`WEEKEND_RANGE`，本表是它們的鏡像；分組名必須與 `QUOTA` 的鍵逐字相符（全形括號與頓號差一個字就會被判「分組名不在 QUOTA 表」）。改任一邊都要同步另一邊。**週末的判定與理由見 4.1.1；4.1.1 不再重列數字。**
```

原本 §4.1 表格下方那兩句（`下限合計 89 則，**全站目標 95～125 則**。` 與 `**權威值在 check.py 的 QUOTA**，本表是它的鏡像…`）併入上表註解，刪除。

#### 1b. §4.1.1 刪表、只留判定與理由

錨點：`### 4.1.1 週末模式` 之後那張 `| 級別 | 平日 | **週末** |` 表（含以下五列與 `scripts/check.py 會自動依 window.from 的星期切換…` 那段）→ 換成：

```markdown
**數字見 4.1 的表**（平日／週末兩欄），本節不重列。`scripts/check.py` 會自動依 `window.from` 的星期切換並在輸出標明「模式：平日／週末」，**判定交給腳本**。
```

保留：判定規則那段（`**判定：窗口起點那一天是週六或週日 → 套用週末下限。**` 起）、`**為什麼要有這個模式**`、`**核心組不放寬**`、校準那段。

#### 1c. §5 配額表刪「下限」欄

錨點：`| 子類別 | 平日 下限 → 配額（誰交多少） | 週末 下限 → 配額（誰交多少） |` 這張表。

- 表頭改為 `| 子類別 | 平日 配額（誰交多少） | 週末 配額（誰交多少） |`
- 每一列刪掉 `N → ` 的前綴，例如 `| 美股與財報 | 10 → **12**　A6・E6 | 10 → **11**　A4・E5・C2 |` → `| 美股與財報 | **12**　A6・E6 | **11**　A4・E5・C2 |`
- 表格下方 `**成卡下限的權威值仍在 check.py 的 QUOTA／WEEKEND_QUOTA**，本表的「下限」欄是它的鏡像。` → 改為 `**配額一律高於成卡下限（下限見第 4.1 節），差額就是汰除餘裕。**`

**驗證**：`grep -c "≥10" AGENT_BRIEF.md` 應由 8 降到 3 上下；`grep -n "→ \*\*" AGENT_BRIEF.md` 應為 0；`check.py` exit 0。

---

### 項目 2 · 排程 prompt 八處殘留規格數字改指標

**問題**：v23 的開場白寫著「**本 prompt 只寫流程，規格數字一律不抄**」，但實際抄了八處。CHANGELOG v25 已自認是待辦。v22 就是這樣壞的（傍晚改字數沒重推 prompt，正本停在 650–800 兩天沒人發現）。

**做法**：`update_scheduled_task` 是**整份取代**——先 Read 現有全文，改完存快照到 `prompts/2026-08-18-v26.md`。

| # | 位置 | 現況 | 改成 |
|---|---|---|---|
| a | 第 5 步 時間預算 | `**中長摘要降為 350～450 字**` | `**中長摘要改用 brief 第 4 節的降級區間**` |
| b | 第 4 步 | `**深度卡 6～8 張；其餘為中長摘要＋2～3 bullet。**` | `**深度卡張數與卡片字數區間見 brief 第 4 節。**` |
| c | 第 4 步 v17 | `（一天 3～6 張，**沿用 index.json 前幾天的既有代號**）` | `（張數見 brief 第 3.5 節，**沿用 index.json 前幾天的既有代號**）` |
| d | 第 4 步 v18 | `` `note` ≤30 字講理由 `` | `` `note` 長度見 brief 第 3.5 節 `` |
| e | 第 3 步 | `**三條上限**：同一種工具連續失敗 5 次熔斷、**同一篇文章失敗 3 次就換篇**、同一來源文章頁 15 篇收工。` | `**三條上限見前言第 2.5 節**（工具熔斷／同篇上限／同來源文章頁上限）。` |
| f | 第 3 步 F 組 | `（內部分配：鉅亨 ≥4、MoneyDJ ≥3）` | 刪除（brief 第 5 節已有） |
| g | 第 3 步 | `**補位上限 2 輪**（8/10 跑了四輪、佔全輪成本 22.6%）。` | `**補位上限見 brief 第 5 節。**` |
| h | 三處 `25 家` | frontmatter description、第 3 步標題、第 4 步 `來源家數寫 25 家` | frontmatter 保留（那是給人看的任務描述）；第 3 步標題改為 `## 第 3 步：採集（7 組**分兩批**派工）`；第 4 步改為 `來源家數依 brief 第 1 節` |

**同時保留不動**：`4～6 格`（那是 Chrome 環境事實，不是本系統的規格）、`8f82131f-…` deviceId（brief 第 6 節也有，但選瀏覽器是 prompt 的第一個動作，這份副本值得留）。

**驗證**：新 prompt 全文 `grep -E "350～450|6～8 張|3～6 張|≤30 字|5 次熔斷|≥4、MoneyDJ|2 輪"` 應為 0 命中；快照檔已存在 `prompts/2026-08-18-v26.md`。

---

### 項目 3 · brief 第 47 行徽章代碼逐字清單

**問題**：26 個代碼寫死在每天完整讀一次的檔裡，自稱是四處的鏡像，來源異動時要同步四份。這是**便宜查詢的快取**（`grep SRCOK scripts/check.py` 即得），且 `src` 不在清單已由 `check.py` 硬擋。

**錨點**：`來源徽章 class：` 那一整行 → 換成：

```markdown
**來源代碼的唯一權威在 `scripts/check.py` 的 `SRCOK`**（另有三份必須與它一致的渲染副本：`index.html` 的 CSS 與 `BADGE`、`search.html` 的 `BADGE`；`index.html` 的 `SRCBAR` 只放現役來源，故不含已停用代碼）。卡片的 `src` 值＝徽章 class 去掉 `b-` 前綴。**`reuters` 與 `ked` 是已停用代碼，保留供歷史封存渲染，不要用在新卡片上；`pub` 保留給清單以外的通訊社與官方公告——FRED、FDA、WGC 的數據卡都用 `pub`。**
```

原本括號內那句 `（卡片 src 值＝去掉 b- 前綴：…）` 一併刪除（已併入上文）。

**驗證**：`python3 -c` 解析四處仍一致（腳本見本檔最後）；`check.py` exit 0；17 個 `data/*.json` 的 `src` 全在 `SRCOK` 內。

---

## 第二批 · 沿革下沉

**原則**：**規則留在 brief，案發現場移進 `CHANGELOG.md`。** 移走的段落逐字貼進 CHANGELOG 對應版次的明細下方（標「（2026/08/18 自 brief §X 下沉）」），不要摘要、不要改寫——那些細節的價值就在具體。

**判準**：一句話同時承載「規則」與「哪天為什麼」時，**留規則、移故事**；純故事的整段整段移走。

### 項目 4 · §3.5 封存架構（~1,800 字元）

schema 欄位定義**全部保留**，只移敘事。

| 行 | 處置 |
|---|---|
| L241 `thermo.level 必須是 0–100 的整數字串` | **留規則**，刪掉 `8/11 前有幾天寫成散文（「偏熱但分歧擴大」），時間序列因此斷點——check.py 自 2026/08/11 起強制檢查。` |
| L248 `「窗口外損失」要先查證再寫進 run 欄` | **留規則整句**（它就是規則） |
| L250 `**相鄰窗口刻意重疊 2～5 小時…**` 整段（265 字元） | **整段移走**。規則已在 §3 的時間軸圖與 L248 講過，這段是 8/16 Jane Street／Nvidia 的案發現場 |
| L256 `run 欄第一句必須寫…` | **留第一句規則**，刪掉 `這套系統幾乎每天改規格…8/12 與 8/13 同一份規格、每則成本差 74%，現在已經無法回頭歸因。` |
| L257 `**取法：直接 Grep…**`（368 字元） | **留「取法」那一句與「取不到就寫未確認」**，刪掉 `（2026/08/17 實測：Glob 掃回 100 條路徑、花掉 0.35M 有效 token，佔當輪主對話 8%…）` 與 8/14 寫錯的那段 |
| L258 `access 欄只寫「本次的來源構成」` | **留規則**，刪掉 `2026/08/06 起移除原本那句「均在使用者本人訂閱…」` 之後的沿革 |
| L240 `snap 每格另附 num 與 chgPct`（255 字元） | **全留**（末句「別再讓數字被鎖在 HTML 字串裡」是意圖，不是沿革） |
| L242・L243 `pulse`／`watchReview` | **全留**（都是欄位定義與判定規則） |

### 項目 5 · §1.1／§1.1.1（~1,600 字元）

| 行 | 處置 |
|---|---|
| L69 `不要用導覽列有沒有「Sign In」來判斷` | 見**項目 9**（改正向），沿革 `2026/08/03 那一輪就是因此誤判` 移走 |
| L73 輪詢那條（218 字元） | **留「改用輪詢」的規則**，刪掉 `固定 4 秒在 2026/08/06 被證實不夠：Barron's 那篇 AMD…**Barron's 已經連續三次以不同形式製造誤判**（8/03…8/05…8/06…）` |
| L82 Barron's 與 IBD 的規則（270 字元） | **留篇數與判定規則**，刪掉 `（2026/08/05 實測：兩家全部可讀、0 篇被擋，Barron's 8 篇 11–22 段／2,382–4,439 字…）` |
| L84 NYT 診斷梯度開場（213 字元） | **留「依序試完這四步並把結果寫進 run 欄」**，刪掉 8/05 的現場描述 |
| L93 `NYT 的阻擋會自癒`（245 字元） | **留「四步診斷照跑、記錄、當日放棄即可，隔天會自己好」**，刪掉兩次案例的日期與段數字數 |
| L99 `這條規則是 Bloomberg 的教訓換來的`（206 字元） | **整段移走**。規則在 L97、上限在 L103，這段純故事 |
| L101 `Bloomberg 在此之前八天分別交出 24、27…` | **留「自傷型風控 vs 進門就擋，處置方式不同」那句**（那是判準），刪掉八天的數字 |
| L104〜L106 三條 bullet | **留 L104 的「列表頁通、文章頁擋＝風控特徵」判準**；L105 留「不要靠重試」、刪括號內實測；L106 `風控約 2～3 小時自行解除` **整條移走**（採集員讀不到 brief，這條對 writer 無用；preamble 若沒有就補進 preamble） |
| L108 `Bloomberg 的列表頁沒有時間戳，要走 __NEXT_DATA__`（248 字元） | **整段移進 `scripts/subagent_preamble.md` 第 3.5 節 Bloomberg 那列**——這是採集眉角，住在 brief 是 §1.1.2 那個坑的殘餘 |
| L110 `附帶發現：部分 Bloomberg 文章頁的 article:published_time 是空的` | 同上，移進 preamble |

---

## 第三批 · 語言層

### 項目 9 · Negation 轉正向

brief 84 次否定詞。**硬護欄保留**（不跑 git、不繞付費牆／鏡像站、不刪改既有 `data/*.json`、不回撈前一版舊卡），但配上正向目標。其餘改寫，優先這幾條：

| 位置 | 現況 | 改成 |
|---|---|---|
| §1.1 L69 | `**不要用導覽列有沒有「Sign In」來判斷能不能讀。**` | `**能不能讀，只看實際取到的內文量**——段落數與字數。Bloomberg 與 Barron's 的導覽列永遠顯示 Sign In／Subscribe Now，內文照樣完整載入。` |
| §1.1.1 L97 | `**不要為了看發布時間而逐篇點進文章頁。**` | `**發布時間一律從列表頁一次取回**：用 `scripts/list_timestamps.js` 取得全部「連結＋時間戳」，篩出窗口內的再點進去讀正文。` |
| prompt 第 3 步 | `**⚠ 六條會浪費整組配額的坑**` 整段（六條全是禁令） | 標題改 `**⚠ 六條保住整組配額的紀律**`；逐條改正向（`絕不用 tabs_context_mcp 的第一個分頁` → `一律帶自己 create 的 tabId`，正向版其實已寫在同一條裡，刪掉禁令版即可） |
| prompt 第 3 步 | `**讀新聞一律 navigate＋read_article.js，不要用 fetch**` | 刪掉後半句（前半句已是正向且完整） |

### 項目 10 · No-op 刪整句

**判準是「相對模型預設有沒有改變行為」，失敗就刪整句、不要修字。**

| 位置 | 句子 | 處置 |
|---|---|---|
| §0 L15 | `**絕不可為了湊數而放舊聞或編造內容**。` | **刪**（§3 L192 已有完整版且帶做法） |
| §4.1 L303 | `這正好違背「不為湊數放舊聞或編造」的底線。` | **刪**（同上；該句主張「一律 ≥10 會逼出注水」本身留著） |
| prompt 第 4 步 | `**絕不放舊聞、不編造、不回撈前一版湊數**。` | **保留**（prompt 是流程檔，這是它唯一一份；brief §3 L527 的「不回撈舊卡」也保留，兩者讀者不同） |
| §5 | `有降級就明說，不要粉飾。`（在 prompt 第 6 步） | **刪**（模型預設就會照實回報；8/17 的 `run` 欄實證主動報了四項缺口） |

**保留不刪**：§7 的「非投資建議」（合規性質，不是行為指令）。

### 項目 11 · 鑄兩個 leading word

**① `擋源三分`** —— `工具失效／被付費牆擋／被風控擋` 這個三元組目前在 `MAINTENANCE.md` §3.1、`subagent_preamble.md` §2.5、brief §1.1、排程 prompt 各展開一次。

- **定義站**：`subagent_preamble.md` §2.5（採集員是唯一要當場做這個判斷的人），一句話定義＋三種處置。
- 其餘三處改為用詞 + 指標：`回報時做**擋源三分**（見前言 §2.5）`。
- `自傷型風控` 這個既有的好詞納入定義站，作為「被風控擋」底下的兩種之一（另一種是「進門就擋」）。

**② `單錨`** —— v24／v25 的核心結論「一個數字只能有一個家」目前用整段散文講三次（brief §4、CHANGELOG v24、v25）。

- **定義站**：`MAINTENANCE.md` §2「修改的標準流程」加一條：
  > **單錨**：一個數字（或一個性質形容詞）在整套系統裡只能有一個家。權威在腳本、說明在 brief、prompt 只指路。**改任何數字之前先問：這個值現在有幾份副本？** 8/17 的字數 bug 就是 §4 同時列著四個歷代區間、執行端挑錯了一個。
- brief §4 那段自述（`**本節是全檔唯一規定「一張卡該多長」的地方**…`）改為 `**本節是卡片長度的單錨**（見 MAINTENANCE §2）。`
- 日後稽核只需一句「跑一次單錨檢查」。

---

## 第四批 · 大手術（規格到計畫層，逐字文案等做的當下再定）

**這三項刻意不預寫逐字文案**：它們動到採集流程，正確的切法取決於前三批做完之後 brief 長什麼樣，先寫死等於自己製造一份會過期的抄本。

### 項目 6 · §5 沿革下沉（~3,000 字元，建議拆兩輪）

§5 佔全檔 34.8%，其中 37% 帶日期。候選行（2026/08/17 狀態，合計 4,205 字元）：

`L364`（8/11 分批派工的成本數字）・`L368`（眉角表只貼該組那幾列的理由）・`L401`（配額對齊歷史實交量）・`L405`（第 9 次修訂漏指派央行組）・`L411`（8/06 央行組零緩衝成因）・`L429`（MOPS 舊路徑連三輪被重導）・`L438`〜`L439`（法說會兩個端點的實測細節）・`L441`（櫃買指數終於有解）・`L443`（8/13 navigate JSON 失敗）・`L451`（8/14 CORS 實測）・`L456`（CME 改 Investing.com）・`L461`〜`L466`（SPDR／ETF 折溢價／COMEX 停追／WGC 要登入）・`L496`（7 個同名檔案）・`L525`（8/10 四輪補位）

**保留判準**：凡是「照著做的規則」與「判斷要用的判準」留；凡是「哪天、幾則、花了多少 token」移走。**`L405` 的「素材從來不缺，缺的是有人負責」那句留著**——它是規則的靈魂，不是故事。

**建議切法**：第一輪只做 L364・L368・L401・L405・L411・L496・L525（派工與配額段）；第二輪做 L429〜L466（數據源段），因為第二輪會和項目 7、8 撞在一起。

### 項目 7 · §5 的 reference 下推

台股六端點表、央行取材面六項、彙整型拆卡規則 → 移出 brief，成為側檔＋指標，brief §5 只留 steps。

**先決定去哪**（做的當下再定，選項）：新開 `SOURCES.md`／併進 `scripts/subagent_preamble.md`／拆成 `scripts/endpoints.md`。**選之前先問：誰在執行的當下需要它？** ——這正是 §1.1.2 那條教訓。

### 項目 8 · 台股端點移進 preamble

現況：六個端點的網址只住 brief §5，而 F 組 subagent 讀不到 brief，靠主 agent 每天轉抄進派工提示。preamble 目前只有一句 CORS 規則、沒有網址。

**與項目 7 一起做**，否則端點表會被搬到一個 F 組更讀不到的地方。

---

## 每批做完的驗證清單

依 `/maintain` 第 4 步（`MODIFY.md`）。**全數通過才算完成，任何一項失敗就回上一步。**

```bash
cd /Users/kenny/advisory-knowledge-hub
python3 scripts/check.py <最新日期>; echo "exit=$?"     # 必須 0
python3 scripts/metrics.py --groups >/dev/null; echo "exit=$?"  # 必須 0
```

```python
# 徽章四處一致（項目 3 之後尤其要跑）
import re
h=open('index.html').read(); s=open('search.html').read(); c=open('scripts/check.py').read()
b=lambda t:set(re.findall(r'(\w+)\s*:\s*"',re.search(r'const BADGE=\{(.*?)\};',t,re.S).group(1)))
css=set(re.findall(r'\.b-([a-z]+)\b',h))
srcok=set(re.findall(r"'([a-z]+)'",re.search(r'SRCOK=\{(.*?)\}',c,re.S).group(1)))
assert css==b(h)==b(s)==srcok, '徽章四處不一致'
```

```python
# 既有封存全部可載入、src 合法
import json,glob,re
c=open('scripts/check.py').read(); ok=set(re.findall(r"'([a-z]+)'",re.search(r'SRCOK=\{(.*?)\}',c,re.S).group(1)))
for f in glob.glob('data/2026-*.json'):
    d=json.load(open(f))
    for s in d.get('sections',[]):
        for g in s['groups']:
            for k in g['cards']: assert k['src'] in ok, (f,k['src'])
```

- brief 的節次數量相對改動前沒有意外掉落（`grep -c "^#\{2,3\} " AGENT_BRIEF.md`，2026/08/17 實測為 17）。
- brief 總長變化落在該批預估範圍內（改動前 77,314 位元組）。**遠超預估＝正則吃過頭，立刻停下。**
- 動到 prompt：`prompts/YYYY-MM-DD-v{N}.md` 快照已存。
- **不要跑任何 git 指令**。要確認落地用 `grep` 工作區檔案、比對 `wc -c`；要確認進版讀 `.git/logs/HEAD` 與 `.git/refs/`。
- `CHANGELOG.md` 最上方已加一筆，並更新版本總覽表，寫清楚**為什麼改**。

---

## 這次刻意不動的

- **§3 窗口定義**、**§4.1.1 的週末下限數值**、**`check.py` 的三張表** —— 行為規格，這輪只碰說明。
- **§4 的字數段** —— 8/18 正在驗錨點假說。若 8/18 判讀顯示錨點修對了，這一段就是「單錨」的示範案例，更不該再動。
- **§3.5 的 schema 欄位定義** —— 只砍該節的敘事，欄位一個都不動。
