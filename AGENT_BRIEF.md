# AGENT BRIEF — 投顧知識庫儀表板・每日重生標準說明

這份文件是「投顧知識庫儀表板」的完整規格。任何一個新的 Cowork 對話（或換裝置）讀了這份，就能完整重現整套系統。使用者說「**跑今天的儀表板**」時，即依本文件執行。全程使用繁體中文（台灣用語），讀者為投顧從業人員。

---

## 0. 觸發與環境前提（重要）

- **執行機器＝家中的 MacBook Pro（使用者名稱 `kenny`）**，該機 24 小時開機，**一週七天每日更新（含週六日與國定假日）**。辦公室那台已停用（launchd 代理已 unload），不再作為發布機器。
- **固定排程：每天台北時間 07:30**，排程任務 ID＝`advisory-dashboard-daily`。另一條線 `podcast-digest-daily` 排在 **03:00**（2026/08/03 起，原為 09:00），兩者刻意錯開以分散 token 用量。**它已改用 podfetch＋Gemini API、不再使用 Chrome，所以「搶 Chrome」不再是錯開的理由。**
- 執行前提：**該台 Mac 醒著、Claude 桌面版開著、`advisory-knowledge-hub` 已加入連線資料夾、Chrome 外掛已連線且各付費站台維持登入狀態。**
- **機器與電源設定見 `MAINTENANCE.md` 第 6 節。** 那台 Mac 需要從 00:55 連續醒著到中午（涵蓋 01:00 轉錄、03:00 podcast、07:35 本線至 11:30），靠的是 `pmset -c sleep 0` 加排定喚醒，且**不裝任何第三方充電管理軟體**。這組設定不存在於任何設定檔中，重灌或換機不會遷移。
- 讀付費訂閱要靠 Claude in Chrome 附著於對話；若該次執行連不到瀏覽器，只能以免費公開來源產出，並在「關於與方法」註明本次未涵蓋付費來源。
- 觸發方式有二：(a) 使用者在互動對話說「跑今天的儀表板」；(b) 每日 07:30 排程自動觸發（排程開的是**全新對話、沒有任何記憶**，所以一切以本文件為準）。
- **週末與清淡日不得跳過。** 新聞量少時改用第 3 節的「前瞻／最新一次」框架把版面補滿，並在心得中誠實說明當日是清淡日；**絕不可為了湊數而放舊聞或編造內容**。

---

## 1. 主要來源（25 家，付費為主體、免費為輔）

**付費訂閱**（使用者本人訂閱，於其已登入的 Chrome 讀）：
Bloomberg (bloomberg.com/asia)、WSJ (wsj.com)、NYT (nytimes.com/international)、FT (ft.com)、Nikkei Asia (asia.nikkei.com)、Washington Post (washingtonpost.com)、Barron's (barrons.com)、IBD (investors.com)、**Politico (politico.com)**、**The Hill (thehill.com)**、**SemiAnalysis (newsletter.semianalysis.com)**、**The Economist (economist.com)**

**免費公開**：
CNBC (cnbc.com/world)、MarketWatch (marketwatch.com)、Tom's Hardware (tomshardware.com)、Oil & Gas Journal (ogj.com)、華爾街見聞 (wallstreetcn.com)、**鉅亨網 Anue (news.cnyes.com)**、**MoneyDJ (moneydj.com)**、**Fierce Biotech (fiercebiotech.com)**、**STAT News (statnews.com)**、**Korea Herald (koreaherald.com)**、**Mint (livemint.com)**、**TrendForce 集邦 (trendforce.com.tw / trendforce.com)**

**官方／數據源**：**TWSE 台灣證券交易所與公開資訊觀測站 (twse.com.tw / mops.twse.com.tw)**、Fed／BOJ／ECB／日本財務省、EIA、CME FedWatch、美國財政部、公司 IR、政府與司法機構公告；**FRED 的 ICE BofA 信用利差（OAS）系列**、**FDA 藥證與臨床公告**、**World Gold Council 黃金 ETF 流向與央行購金**；另有 AP、CBS 等通訊社。

**G 組四家的實測定位**（2026/08/05 首次啟用後修訂）：

- **Fierce Biotech＝生技健護的主力**。免費、全文可讀，8/05 實測 7 篇全數完整取得。
- **STAT News＝輔助，不是支柱**。免費新聞篇數偏少（8/05 窗口內 8 篇有 5 篇是 STAT Plus），**每天收到 1～2 則就算正常**。注意它的付費文在 DOM 上仍有 16 段／2,181 字，但扣掉記者簡介與訂閱區塊後實際內文只剩約 3 段／600 字——**依第 1.1 節判定為被擋就換篇，不要硬讀**。
- **Korea Herald＝韓國主力**（2026/08/05 起取代 KED Global）。**KED Global 對未登入讀者只提供導言**：`section.news-body` 一律只有 255～360 字元、`<p>` 標籤 0 個，而且頁面上**沒有任何攔截字串**——這種「安靜的截斷」最危險，內文量標準會判定為被擋，但執行者若只看有沒有訂閱提示就會誤以為讀到了。Korea Herald 為全文免費，改用它當韓國的主力來源。
- **Mint＝印度主力**，8/05 實測完整可讀，印度題材可獨立達標。
- **東南亞不另設具名來源**：Nikkei Asia 的東南亞覆蓋已足夠。

**`ked` 這個徽章代碼保留但不再使用。** 2026/08/05 那一版有 3 則 KED 卡片，代碼從 `index.html` 的 `BADGE` 與 CSS 移除會讓那天的封存檔渲染出沒有樣式的空徽章。**歷史封存不可回頭修改，所以舊代碼一律只從 `SRCBAR`（頁面上的來源列）移除、CSS 與 `BADGE` 永久保留。** 檢查腳本的 `SRCOK` 白名單同理保留 `ked`，否則拿舊檔重跑檢查會誤報。

**Politico 與 The Hill 專供政經分頁**（地緣政治＋美國政治）的深度與時效——國會立法、國防／撥款、選舉、司法與 Fed 獨立性等。

**2026/08/06 第 12 次修訂新增的兩家**：

- **TrendForce 集邦（`tf`）＝記憶體、面板、功率半導體的一手報價與產能追蹤。** 動機是 **DRAM／NAND 價格至今全部靠二手轉引**——媒體引用的數字往往落後一至兩週、且經常沒寫清楚是合約價還是現貨價。TrendForce 是這些報價的原始發布方。**中文站 `trendforce.com.tw` 免費且更新較勤，英文站 `trendforce.com` 內容較少**，以中文站為主。歸 **D 組**，主要餵「AI 與半導體」（核心組 ≥10，8/05 曾剛好卡在 10 則零緩衝），次要餵「能源與原物料」（功率半導體、太陽能供應鏈）。
  **報價卡的口徑一定要標清楚**：合約價 vs 現貨價、DDR4 vs DDR5、顆粒 vs 模組、月度 vs 週度——這正是二手轉引最常搞混的地方，也是收這家的意義所在。
- **The Economist（`econ`）＝全球宏觀與政治經濟的框架性分析。** 它提供的不是速報而是**解釋框架**，對每月 House View 的歐洲、地緣政治、央行三節價值最高。歸 **B 組**。**週刊節奏的特殊處理見第 1.3 節。**

來源徽章 class：`b-bbg`/`b-wsj`/`b-nyt`/`b-ft`/`b-nikkei`/`b-wapo`/`b-barrons`/`b-cnbc`/`b-ibd`/`b-mw`/`b-toms`/`b-ogj`/`b-politico`/`b-thehill`/`b-wscn`/`b-anue`/`b-moneydj`/`b-twse`/`b-semi`/`b-fierce`/`b-stat`/`b-kh`/`b-mint`/`b-tf`/`b-econ`/`b-pub`（另有已停用但保留的 `b-ked` 與 `b-reuters`）。**權威在三個檔案**：`index.html` 的 CSS 與 `BADGE`、`search.html` 的 `BADGE`、`check.py` 的 `SRCOK`——本行是它們的鏡像，來源異動時四處一起改。
（卡片 `src` 值＝去掉 `b-` 前綴：`bbg`、`wsj`、…、`twse`、`semi`、`fierce`、`stat`、`kh`、`mint`、**`tf`**、**`econ`**、`pub`。`pub` 保留給上列以外的通訊社與官方公告——**FRED、FDA、WGC 的數據卡都用 `pub`**。**`reuters` 與 `ked` 為已停用代碼，保留供歷史封存渲染，不要用在新卡片上**。）

各來源擅長：Bloomberg＝全球即時＋亞洲；WSJ／NYT＝美國政經；FT＝全球／科技／市場；Nikkei＝亞洲供應鏈／匯率；WaPo＝美政治／地緣；Barron's／IBD／MarketWatch＝美股與選股視角；Tom's Hardware＝半導體/GPU/資料中心；OGJ＝油氣/LNG；華爾街見聞＝中文彙整西方財經＋亞洲；**鉅亨網＝台股盤勢與台灣本地財經**；**MoneyDJ＝台廠供應鏈與個股拆解**；**TWSE／公開資訊觀測站＝月營收、三大法人買賣超、融資餘額、法說會行事曆等官方數據**；**SemiAnalysis＝AI 資料中心、加速器架構、記憶體與 CoWoS 供應鏈的第一手深度拆解，數字顆粒度是其他來源沒有的**；**Fierce Biotech／STAT＝新藥、臨床試驗、FDA 與藥價政策**；**Korea Herald＝韓國**；**Mint＝印度**；**TrendForce 集邦＝記憶體／面板／功率半導體的一手報價與產能追蹤**；**The Economist＝全球宏觀與政治經濟的框架性分析，週刊節奏見第 1.3 節**。

**新增子類別對應的主力來源**（2026/08/04 第 9 次修訂，這張對照表是為了避免「加了子類別卻沒人負責去餵它」）：

| 子類別 | 主力來源 | 保底數據源 |
|---|---|---|
| 中國 | 華爾街見聞、FT、Nikkei、Bloomberg | — |
| 日本 | Nikkei（最強）、Bloomberg、FT | BOJ、日本財務省 |
| 亞太（韓／印／東南亞） | **Korea Herald**、**Mint**、Nikkei | — |
| 歐洲 | FT（最強）、**The Economist**、Bloomberg、WSJ | ECB |
| 生技健護 | **Fierce Biotech**、**STAT**、WSJ、FT | FDA 公告 |
| AI 與半導體 | Tom's Hardware、SemiAnalysis、**TrendForce**、Bloomberg、FT | — |
| 信用債 | Bloomberg（credit 版面最強）、FT、WSJ | **FRED 的 ICE BofA OAS** |
| 黃金 | Bloomberg、MarketWatch、Barron's | **SPDR GLD＋GLDM 持倉＋ETF 折溢價** |

**內容優先序**：每個分區以付費來源卡片打底、排前面；免費來源只補付費沒涵蓋到的角度。**核可的免費來源僅限**上列十二家＋官方／數據源／通訊社；**嚴禁**內容農場或小報（Motley Fool、ETtoday、Intellectia、內容聚合站等）。

### 1.1 讀取方法與付費牆判斷（2026/08/03 修訂，很重要）

**不要用導覽列有沒有「Sign In」來判斷能不能讀。** 這是錯的判準，2026/08/03 那一輪就是因此誤判。實測：Bloomberg 與 Barron's 的導覽列**永遠**顯示 Sign In／Subscribe Now，但文章內文照樣完整載入。

**正確的判斷方式——用實際取到的內文量：**

1. `navigate` 之後**不要用固定秒數等待，改用輪詢**——見下方片段。固定 4 秒在 2026/08/06 被證實不夠：Barron's 那篇 AMD 報導等 4 秒只有 2 段／318 字（**看起來就像被擋**），補等 5 秒後變 10 段／2,563 字。**Barron's 已經連續三次以不同形式製造誤判**（8/03 用 Sign In 判斷、8/05 選擇器沒對上、8/06 等待不足），固定秒數治不了根本。
2. **主要讀法是 `javascript_tool`，程式碼已外部化為 `scripts/read_article.js`（唯一權威版本）**——同一次呼叫抓內文＋發布時間（`ts` 是窗口制的地基，漏抓等於這篇不能用）。**主 agent 每天用 Read 讀該檔一次，逐字放進每個 subagent 的提示**；改選擇器只改該檔（各段選擇器的由來見檔內註解）。
3. **判定標準**：段落數 ≥8 且內文字數 ≥1,500 → 視為完整取得，正常成卡。
   段落數 ≤3 或內文字數 <800，**且**頁面出現明確的攔截字串（Barron's 的 "Continue reading this article with a Barron's subscription"、WSJ 的訂閱牆元件）→ 才算真的被擋。
   **段落數少的時候，先懷疑選擇器沒對上，再懷疑被擋。** 判定被擋之前，至少換一組選擇器（`p[class*="Paragraph"]`、`[class*="content"] p`、`div[data-testid] p`）重試一次。

3.5 **`fetch()` 取 HTML 再解析 ≠ 實際開頁（2026/08/05 補）**。IBD 實測：用 `fetch()` 拿 HTML 只會得到付費牆前的導段（7 段／987 字），**必須真的 `navigate` 過去、在頁面上執行 JS 才拿得到完整內文**（27 段／5,939 字）。許多站的正文由前端在登入狀態下才注入，靜態 HTML 裡根本沒有。**掃列表可以用 `fetch`，讀正文一律 `navigate`。**
4. **`get_page_text` 在 Bloomberg、Barron's、MarketWatch 上會嚴重低估內文**（只回傳前 1–3 段或側欄）。它只適合當快速掃標題用，**不可以拿它的結果來斷定文章被付費牆擋住**。

**Barron's 與 IBD 的規則**：依上述標準實測後再決定。**確認被擋才**停止逐篇嘗試、只取首頁公開資訊且不單獨成卡，並在 `run` 欄記錄。**可正常讀取時，Barron's 讀 6–8 篇、IBD 讀 4–6 篇**——這兩家的美股選股與週展望視角是其他來源沒有的，不要輕易放棄。（2026/08/05 實測：**兩家全部可讀、0 篇被擋**，Barron's 8 篇 11–22 段／2,382–4,439 字，IBD 5 篇 21–80 段／3,400–15,324 字。前提是用對選擇器與 `navigate`。）

**NYT 的診斷梯度（2026/08/05 新增）**：8/05 那輪 11 篇實測 `document.body.innerText.length` **全為 0**、`document.title` 停在 `"nytimes.com"`——**這不是付費牆截斷，是頁面完全沒渲染**。使用者同日以同一台機器手動開啟則完全正常，所以**不是帳號或訂閱問題**。判定為硬阻擋之前，依序試完這四步並把結果寫進 `run` 欄：

1. **等久一點**：把等待由 4 秒拉到 8–10 秒再讀（NYT 前端較重，4 秒可能不夠）。
2. **先開首頁再點進去**：直接 `navigate` 到文章 URL 有時會卡在反機器人的前置檢查；改成先開 `nytimes.com/international`、確認渲染成功後再從列表點連結。
3. **換一個分頁**：關掉當前分頁、開新分頁重試（8/04 有過分頁狀態卡住的前例）。
4. **確認不是整站**：試 `nytimes.com` 首頁本身，`innerText.length` 若也是 0，代表整站層級的阻擋，才記為當日不可用。

**四步都失敗才算硬阻擋**，當次放棄、`run` 欄如實記錄，**不得使用任何鏡像站或快取**。B 組少了 NYT 時，把「美國政治與政策」的缺口交給 E 組（Politico＋The Hill）吸收，「歐洲」與「金融併購」則由 FT 加倍補位。

**NYT 的阻擋會自癒，不要為它改動任何規則**（2026/08/09 更新）。已出現兩次完全相同的模式：**排程時段文章頁全 0 字元、同日稍晚手動測完全正常**。8/05 全 0 → 8/06 恢復；8/09 全 0（四步診斷全跑完仍是 0）→ 同日 09:50 實測 **37 段／11,271 字、時間戳正常、3 秒就穩定**，且「從列表頁點進」與「直接 navigate」兩種方式都通。中間 8/06～8/08 都有產出。**四步診斷照跑、記錄、當日放棄即可，隔天會自己好。**

### 1.1.1 預篩時間戳一律從列表頁做（2026/08/09 第 15 次修訂，重要）

**不要為了看發布時間而逐篇點進文章頁。** 用 `scripts/list_timestamps.js`：列表頁一次就能取回全部「連結＋時間戳」，篩出窗口內的再點進去讀正文。

**這條規則是 Bloomberg 的教訓換來的。** 8/09 那輪 A 組在預篩階段對 Bloomberg **連續發出約 35 次請求**，觸發網路層風控，此後所有文章頁一律回傳「Are you a robot?」（4 段／464 字），當日 **Bloomberg 成卡 0 篇**——而開場的可用性測試原本是**通過的**（23 段／5,208 字）。**是採集方式把來源惹毛的，不是來源本來就擋。**

Bloomberg 在此之前八天分別交出 24、27、27、14、20、19、15、17 則，一直是全站最大來源。**這種「自傷型風控」與 Reuters 那種「進門就擋」完全不同，處置方式也不同**：Reuters 該移除，Bloomberg 該改採集方式。

- **單一來源的文章頁請求上限 15 篇**，逼近就收工回報，不要為了多讀幾篇賭風控。
- **列表頁本身不受影響**——8/09 實測 Bloomberg 列表頁在文章頁全被擋的同時仍然正常（15,729 字元、101 條連結）。所以「列表頁通、文章頁擋」這個組合就是風控被觸發的特徵，不是站台掛掉。
- 觸發風控後**不要靠重試解決**（8/09 間隔重試四次、另開新分頁均未恢復，40 分鐘後仍被擋）。當次放棄、如實記錄、交給補位。
- **風控約 2～3 小時自行解除**（8/09 實測：約 08:00 觸發 → 09:50 仍被擋 → 10:35 完全恢復，同一篇文章從 4 段／464 字回到 23 段／5,208 字，另測一篇不同文章 17 段／5,031 字）。**但恢復時間已在當日窗口尾聲，救不回當天的產出**——所以重點永遠是不要觸發，不是等它好。

**Bloomberg 的列表頁沒有時間戳，要走 `__NEXT_DATA__`**（2026/08/09 實測）：它的列表頁 33 條連結**全部沒有 `<time>`、也沒有相對時間文字**，DOM 三層備援全部落空。但它是 Next.js 站，`window.__NEXT_DATA__` 裡有 **62 篇的「slug＋標題＋發布時間」**，一次請求就取代 35 次逐篇 fetch。`scripts/list_timestamps.js` 已內建這層備援（DOM 完全取不到時才啟用）。

**附帶發現：部分 Bloomberg 文章頁的 `article:published_time` 是空的**（8/09 實測 Korea Volatility 那篇），此時 `ts` 只能靠列表頁的 `__NEXT_DATA__` 補——**這是另一個非走列表頁預篩不可的理由**。

### 1.1.2 各來源的已知眉角

**已移入 `scripts/subagent_preamble.md` 第 3.5 節（2026/08/09）。** 眉角是**採集員的操作知識**，而採集員只拿到「共用前言＋兩支腳本＋配額」、**不會讀本 brief**——放在這裡等於他們看不到。

**這條教訓值得記住**：判斷一份知識該住在哪，要看**誰在執行的當下需要它**，不是看它「感覺上像規格還是像流程」。早上把這張表從排程 prompt 移進 brief 時就踩了這個坑——動機是對的（單一來源原則），但移錯了地方，等於讓 Korea Herald 的 `/Business`、Mint 的全頁 `p`、Bloomberg 的 `__NEXT_DATA__` 這些資訊對真正要用的人消失。

### 1.2 SemiAnalysis 的特殊處理（2026/08/04 新增，2026/08/16 眉角下推）

**採集眉角（入口與掃連結寫法、選擇器、`ts` 取法、訂閱 CTA 不等於被擋）已移入 `scripts/subagent_preamble.md` 第 3.5 節**——理由同第 1.1.2 節：採集員只拿到共用前言＋兩支腳本＋配額，讀不到本 brief。

本節只留 writer 要用的兩條：

- **發文頻率約每週 1～2 篇。窗口內沒有新文是正常狀態**，不記為降級，收錄範圍也只限窗口內——窗口規則對所有來源一視同仁。
- **有新文的那天通常值得直接寫成「AI 與半導體」那一組的深度卡**（`deep: true`），單篇資訊密度遠高於一般報導。一篇長文可視為彙整型文章，依第 5 節第 7 點的規則**最多拆成 3 張不同子類別的卡**（資料中心電力議題常同時橫跨 AI 與半導體／台股與亞太／能源與原物料）。

### 1.3 The Economist 的特殊處理（2026/08/06 新增，2026/08/16 眉角下推）

**採集眉角（三種節奏與各版取捨、`ts` 為 UTC、The World in Brief 不收）已移入 `scripts/subagent_preamble.md` 第 3.5 節。**

本節只留 writer 要用的兩條：

- **出刊日的窗口特別豐盛、其他日子可能一篇都沒有，兩種都是正常狀態。** 比照 SemiAnalysis：窗口內沒有新文不記為降級，收錄範圍也只限窗口內。
- **出刊日值得寫深度卡**：週刊的 Briefing 或封面故事通常是全站當天最有框架感的素材，適合放在「歐洲」「地緣政治（中東與戰事）」或「央行、利率與匯率」組。

---

## 2. 合規紅線（必守）

- 只讀使用者本人訂閱、在其登入的 Chrome 中合法存取的付費內容；以及免費公開來源。
- **絕不使用任何繞過付費牆或反爬蟲的手段**，包含 archive 鏡像站、快取、CAPTCHA 破解。
- 公開頁面只放「原創重點摘要＋原文連結」，不轉載付費文章全文或逐字複製大段內容。
- 財務數字（單季獲利、資本支出、募資／併購金額、即時報價漲跌）盡量以官方或多來源交叉；不確定者標註。

### 2.1 瀏覽器擴充套件的干擾（2026/08/06 新增）

2026/08/06 那一輪 G 組在 `statnews.com` 頁面偵測到擴充套件（研判為 **Bypass Paywalls Clean**）注入導向 archive 鏡像站的字串。**當輪處置正確**：未點擊、未依循、相關 STAT+ 文章一律以「被擋」處理並換篇。往後照同樣方式處理。

**這件事的重點不只是合規，更是資料可信度**：這類擴充套件的典型行為是**自動改寫頁面**——移除付費牆元素、注入完整內文，有些規則會靜默重導。它在 statnews 上留下了可見字串所以被抓到，但**在 Bloomberg、WSJ、Barron's、FT 上若生效是不會留痕跡的**。後果是「內文量標準」判定的「完整取得」**無法分辨內容來自訂閱還是擴充套件**，而這種污染完全沒有跡象可循。

因此：

- **偵測到任何繞過付費牆類擴充套件的注入痕跡**（archive.ph／archive.today／12ft／freedium 等字串、非預期的「已為你移除付費牆」提示、頁面自動跳轉到鏡像站），一律**當該篇被擋、換篇**，並在 `run` 欄具名記錄是哪一家、哪個頁面。
- **不要因為「反正拿得到內容」就沿用**。規則不是為了頁面上的聲明——**是為了讓「這篇讀得到」這個判斷維持可信**。
- 使用者已知悉此擴充套件的存在；是否停用由使用者決定，執行端一律照上述處置。

**注意**：`about.access` 欄自 2026/08/06 起不再寫合規聲明（見第 3.5 節），**但本節的執行規則完全不受影響**。移除頁面文案與放寬採集行為是兩件事。

---

## 3. 時效性與歷史封存

本儀表板為**日更（一週七天）**，使用者最重視的就是「每天打開都看到最新資訊」。

**核心規則（2026/08/03 第 4 次修訂起）：每一天的版本，全站只收「滾動 24 小時窗口」內發布的新聞，且每一張卡片都是當天新寫的。不再保留、不再改寫任何前一版的舊卡。**

**窗口的精確定義**（要能程式化驗證，不能憑感覺）：

- **起點＝前一日台北時間 07:00**（固定）
- **終點＝本次寫入檔案的時刻**（實務上約當日 11:00～13:00）
- 因此實際窗口長度約 **24～30 小時**，一律以起點為準判斷收不收。

**為什麼起點壓在前一日 07:00，而不是「產出時刻往前推整 24 小時」**：本儀表板在台北上午產出，而**前一晚的美股收盤是當天最重要的素材**——美東前一日 16:00 收盤 ＝ 台北當日 04:00，相關報導多半掛前一日的日期。若用「日期＝今天」或「產出時刻往前推 24 小時」這種浮動起點，會把美股盤後報導整批切掉，那是本末倒置。

**相鄰兩天的窗口會重疊 2～5 小時，這是刻意的容錯，不是 bug：**

```
今天的窗口：昨日 07:00 ──────────────→ 今日 <寫入時刻>
明天的窗口：        今日 07:00 ──────────────→ 明日 <寫入時刻>
                    └── 重疊 07:00–寫入時刻 ──┘
```

重疊區間正好是**今天採集作業正在進行的那段時間**——這段時間發布的新聞很可能還沒出現、或出現了但還沒讀到，今天漏掉是常態。有了重疊，明天可以補收，**不會因為「昨天沒讀到」就永久掉單**。重複成卡的風險由「不得與前一版共用任何原文連結」這條跨版去重規則擋住，兩者是一組配套。

**窗口之間不會有縫隙**：今天 11:30 到明天 07:00 之間（也就是今天下午到深夜）發布的新聞，全部落在明天的窗口內，不會漏。

**這條規則只約束「單一天的版本內部」，不是刪除歷史。** 每天的版本都獨立存成 `data/YYYY-MM-DD.json` 永久保留，使用者可用頁面最上方的日期切換列回看任何一天。**絕對不要刪除或覆寫舊的日期檔。** 每天要做的是**新增一個檔案**，不是改寫昨天的檔案。

- **全站卡片數 ＝ 當日新卡數**，恆為 100%。則數目標與十五組的分級下限見第 4.1 節（窗口起點落在週末時見 4.1.1）。
- **不得從前一版複製任何卡片過來**，即使只差幾小時、即使改寫過。前一版的內容已永久封存在它自己的日期檔裡，讀者要看隨時可切回去。
- **份量下限不可為了時效讓步，時效也不可為了湊足下限而讓步。** 兩者衝突時的唯一解法是**多讀**，不是放寬窗口、更不是回頭撈舊卡。真的某個子類別當天素材不足，見下一條。
- **素材真的不足時，用「前瞻／最新一次」框架補，而不是放舊聞。** CPI、非農、FOMC／ECB／BOJ 決議、OPEC+ 會議等低頻事件本來就不會每天發生；寫成「下次會議前瞻」或「最新一次決議＋至今的市場反應」，引用的是**窗口內的最新分析與市場反應**，`ts` 掛該篇分析的發布時間，不是掛那場會議的日期。這是唯一合法的補版面方式。
- **每張卡片都要標 `ts`（發布時間）與 `date`（顯示用日期）**，發布前程式化檢查所有 `ts` 皆 ≥ 窗口起點。
- 「關於與方法」分頁的時效性說明要寫出**本次窗口的起訖時刻**（例如「2026/08/03 07:00 – 08/04 11:40（台北）」），不再寫「保留的日期區間」。

---

## 3.5 封存架構（2026/08/02 起）

`index.html` 已改為**讀 JSON 的單頁應用**，內容與外殼分離。每天產出的是資料檔，不是 HTML。

```
index.html            # 外殼：CSS ＋ 渲染邏輯 ＋ 日期切換列（約 27KB，很少需要改）
search.html           # 全站搜尋頁（2026/08/10 新增）：讀全部封存做前端搜尋，不需要每日維護
data/index.json       # 封存索引：days 陣列，由新到舊
data/2026-08-02.json  # 每日內容，一天一個檔，永久保留
data/2026-07-30.json
```

**`data/index.json`**：

```json
{ "updated": "2026/08/04 11:40 (台北) · 週二亞洲盤中",
  "count": 4,
  "days": [ { "date": "2026-08-04", "weekday": "週二",
              "stamp": "2026/08/04 11:40 (台北) · 週二亞洲盤中",
              "headline": "當日心得標題", "cards": 87,
              "keptDates": ["2026/08/03","2026/08/04"],
              "thermo": 64, "threads": ["日銀升息路徑","財報季"],
              "file": "data/2026-08-04.json" } ] }
```

（`count` ＝ `days` 的長度，每天 +1。`cards` 在 24 小時窗口制下約 80～95。`keptDates` ＝ 窗口涵蓋到的日期，跨午夜所以通常兩個。）

**`thermo`／`threads`／`watch`／`pulse`／`snap`（第 17、18 次修訂新增，當日 entry 必填）——index.json 是系統的「跨日記憶」**：寫今天的 entry 之前，先讀前幾天的 entry，thread 沿用、pulse 比對翻轉、watch 寫回顧，全部不用打開任何舊的日檔。

- `watch` ＝ 當日 `overview.watch` 的鏡像（`d`/`t` 兩欄即可）。**明天的產出要靠它寫「昨日節點回顧」。**
- `pulse` ＝ 當日 `overview.pulse` 的鏡像（`k`/`dir` 兩欄即可）。前端據此標記立場翻轉。
- `snap` ＝ 當日 `overview.snap` 的數值鏡像（`k`/`num`/`chgPct` 三欄）。前端累積 ≥10 天後自動標「近 N 日最大波動」旗標，**writer 不用做任何異常判斷**。
- 
- `thermo` ＝ 當日 `overview.thermo.level` 的整數值。前端日期列會顯示歷史溫度，跨日就是一條風險偏好時間序列。
- `threads` ＝ 當日全部卡片用到的 `thread` 代號（去重後的陣列，沒有就給 `[]`）。**寫之前先看 `days` 前幾天的 `threads`——同一條故事沿用同一代號**，代號一變，連續天數就斷了。前端據此渲染「延續中的主題 · 第 N 天」。

**`data/YYYY-MM-DD.json`** 的頂層鍵：`date`／`weekday`／`stamp`／`headline`／`window`／`keptDates`／`cards`／`overview`／`essay`／`sections`／`about`。

- **`window`**（2026/08/03 第 4 次修訂新增）：本次收錄的時間窗口，`{"from":"2026-08-03T07:00+08:00","to":"2026-08-04T11:40+08:00"}`。`from` 固定為前一日台北 07:00，`to` 為寫入時刻。
- **`keptDates`**：語意已變。窗口跨午夜，所以它現在就是「窗口涵蓋到的日期」，通常是兩個（前一日與當日）。保留此欄純為向下相容，**前端並未使用**，不要花力氣在它身上。

- `overview`：`snap`（6 格，各含 `k`/`v`/`tone`，tone＝`up`/`dn`/`fl`）、`focus`（4 張，`k`/`v`）、`takeawaysTitle`、`takeaways`（7 條）、`thermo`（`level`/`note`）、`watch`（`d`/`t`）
  - **`snap` 每格另附 `num` 與 `chgPct`（2026/08/10 第 17 次修訂新增，必填）**：`num`＝該指標的數值（如 `7757.64`），`chgPct`＝漲跌百分比數值（如 `0.62`、跌為負；不適用時給 `null`，如殖利率用 bp 表示的情況）。`v` 仍是顯示字串，`num`/`chgPct` 是給機器讀的——下游 House View 與 convergence-weekly 靠這兩欄把每日快照串成時間序列，**別再讓數字被鎖在 HTML 字串裡**。
  - **`thermo.level` 必須是 0–100 的整數字串**（如 `"64"`），質性描述一律放 `note`。8/11 前有幾天寫成散文（「偏熱但分歧擴大」），時間序列因此斷點——check.py 自 2026/08/11 起強制檢查。
  - **`pulse`（2026/08/10 第 18 次修訂新增，必填）**＝五資產每日立場列，固定五筆、順序與鍵名逐字為 **美股／美債／美元／黃金／原油**，各含 `k`、`dir`（只能是 `偏多`/`中性`/`偏空`）、`note`（≤30 字的一句理由）。**這是每日版的迷你 House View**：先看 `index.json` 昨天的 `pulse`，立場翻轉必須是有意識的決定、且 `note` 要講清楚為什麼翻。不確定就寫`中性`，不要天天翻來翻去。
  - **`watchReview`（2026/08/10 第 18 次修訂新增，必填）**＝昨日盯盤節點回顧。從 `index.json` 前一天 entry 的 `watch` 逐條檢視，各含 `d`、`t`（照抄昨天的節點文字）、`verdict`（只能是 `應驗`/`落空`/`未決`）、`note`（一句話講實際發生了什麼，附數字）。**這是本站與一般新聞摘要的分水嶺——拋出去的預測要回頭對答案。** 判定要誠實：模稜兩可就是`未決`，不要把落空硬拗成應驗；`落空`的 note 尤其要寫清楚，那是最有學習價值的部分。前一天 entry 沒有 `watch`（不會發生，已回填）才可為空。
- `essay`：`title`／`by`／`kick`／`paras`（5–6 段）
- `sections`：三個區塊（id＝`macro`/`industry`/`politics`），各含 `title`/`en`/`intro`/`groups`；`groups` 內為 `label`/`accent`（``/`tw`/`macro`/`mat`）/`cards`
- **卡片 dict**：`src`（來源代碼）／`tag`／`tagcls`（``/`hot`/`warn`/`pos`）／`date`（`YYYY/MM/DD`，顯示用）／**`ts`**／`title`／`deep`（bool）／`body`（deep 時為段落 list，否則字串）／`bullets`／`url`／`tone`（`t-green`/`t-yellow`/`t-orange`/`t-red`）／**`thread`（選填）**
  - **`thread`（2026/08/10 第 17 次修訂新增，選填）**＝跨日延續的故事線代號，2～16 字（如「日銀升息路徑」「財報季」）。**只給真正跨日發展的題材**（政策路徑、財報季、併購案進程、戰事），單日事件不要硬掛。**先查 `index.json` 前幾天的 `threads` 沿用既有代號**；一天掛 thread 的卡片以 3～6 張為宜，寧缺勿濫。前端會在卡片顯示代號、在總覽顯示「延續中的主題 · 第 N 天」，convergence-weekly 直接以此欄追主題。
  **「窗口外損失」要先查證再寫進 `run` 欄（2026/08/16 第 22 次修訂新增）**：發現某則重要素材落在窗口起點之前時，**先確認前一版的封存裡有沒有它**，有就不是損失、是跨版去重正確運作，不要寫成降級。

**相鄰窗口刻意重疊 2～5 小時，正是為了接住這種邊界素材**（見上文的時間軸）。8/16 那輪把「Jane Street 150 億虧損差 1.5 小時、Nvidia SpaceX 持股差 32 分鐘」寫成「窗口外遭排除的重要素材」，但兩則其實都在 8/15 的封存裡（`ts` 分別是 08-15T05:30 與 08-15T05:17，落在 8/15 那輪的窗口 08-14T07:00→08-15T10:30 內）——**容錯成功被寫成了失敗**。這種誤記的危險在於：若有人照著回報去「修窗口」，就會把設計對的東西改壞。

- **`ts`（2026/08/03 第 4 次修訂新增，必填）**＝原文的發布時間，ISO 8601 含時區偏移，例如 `"2026-08-04T04:32+08:00"`。**一律換算成台北時間（+08:00）再寫入**，不要混用來源當地時區，否則窗口比對會出錯。
  - 取 `ts` 的方式：優先讀文章頁的 `<time datetime="...">` 或 `meta[property="article:published_time"]`；讀不到就用文章上顯示的日期時間換算；**連日期都只有「X hours ago」這種相對時間時，以擷取時刻往前推該時數估算，並在 `ts` 後面不做任何標記——但若估算誤差可能跨越窗口起點，寧可不收這篇。**
  - `date` 欄仍必填且必須與 `ts` 的台北日期一致（前端只顯示 `date`，不讀 `ts`）。
- `about`：`timeliness`／`notes`（list）／`access`／`run`／`limits`
  - **`run` 欄第一句必須寫「本輪由 <模型名稱> 執行」**（2026/08/13 第 20 次修訂新增）。這套系統幾乎每天改規格，日後比較「哪一版比較好」時，**不知道中間換過模型就會把模型差異誤判成規格改壞了**——8/12 與 8/13 同一份規格、每則成本差 74%，現在已經無法回頭歸因。
    **模型名稱一律取自工作階段的 metadata（`.jsonl` 紀錄的模型欄位），不要憑對話中的自我描述填**——8/14 首日就寫錯：`run` 欄寫「Claude Opus 4.5（依工作階段標示）」，而工作階段紀錄實際是 `claude-opus-5`。**取不到 metadata 就寫「未確認」，不要猜也不要用近似的版本號**；寫「未確認」是可接受的，寫錯的版本號比空著更糟，因為它看起來像有效資料。
  - **`access` 欄只寫「本次的來源構成」**（付費訂閱哪幾家、免費公開哪幾家、官方數據源有哪些），**不要寫合規聲明**。2026/08/06 起移除原本那句「均在使用者本人訂閱、且已登入的 Chrome 中合法讀取，未使用任何繞過付費牆的手段」——這個資料庫是使用者內部參考用途，頁面上不需要對外聲明。**注意：這只改頁面文案，第 2 節的執行層規則完全不變**，理由見該節。

**欄位值可含 `<b>`、`<strong>`、`<span>` 等行內 HTML**，由前端 `innerHTML` 渲染。

**每日發布動作只有兩個**：(1) 新增 `data/<今天>.json`；(2) 把今天這筆 **加進** `index.json` 的 `days` 並依日期由新到舊排序。**不要動任何既有的日期檔。** 若同一天重跑，覆蓋當天那一個檔即可。

---

## 4. 版面結構（六個分頁）

每日產出的是資料。`index.html` 的 CSS、版面、分頁與互動邏輯、徽章 class 一律沿用，只更新資料與 `stamp`（台北時間）。頁面最上方為**日期切換列**（顯示最近 7 天，更早的收進下拉選單）。

**定位＝新聞閱讀中心（reading hub），內容越充實越好。** 各子類別的份量下限見第 4.1 節（分四級，權威值在 `check.py` 的 `QUOTA`）；採「分層摘要」：
- **深度卡**：用 `.card.wide`（跨兩欄）＋ `.longread`（多段落、**約 900～1,300 字**的深度摘要，2026/08/16 由 800～1,000 追認並放寬上緣）＋標 `.tag.deep`「深度」；深入說明事件來龍去脈與對市場的意義。
- **其餘則**：一般 `.card` ＋ `.lead`（**550～900 字**中長摘要）＋ 2～3 條重點 bullet。

  **字數沿革**：250～350（原始）→ 450～600（2026/08/14）→ **650～800（2026/08/16，追認實際產出）**。8/14 提高的理由是實測成本結構——那一輪**採集子代理佔 60.5%、真正讀網頁佔 12.3%，兩者都完全不受卡片長度影響**；撰寫輸出加寫入快取合計只佔 17.3%。**加長既有卡片只付「寫」的錢，多收一則卡片要付「採集＋寫」的錢，而採集是寫的三倍以上——想提高每輪的價值密度，加長比加量划算得多。**

  **8/16 這次是追認而非再次提高**：新規則上路後實際跑出 702 字（8/15）與 722 字（8/16），高出當時上限 20%；深度卡也從 899 漂到 1,080～1,135。**內容變厚是要的，但規格不描述現實就是漂移**——所以把區間對齊到實際水準，並讓 `check.py` 開始量測（見下）。

  **區間在同日傍晚由 650～800 放寬為 550～900**（深度卡上緣 1,200→1,300）。依據是 8/16 的實測分布：103 張一般卡**呈乾淨單峰**——最短 570、中位 700、最長 1,081，650–800 區間內 63 張、兩側各 20／19 張。**40 張逾越純屬自然離散，不是品質問題**；但一個每天都亮的警告很快會被當成背景噪音，**放寬後涵蓋 102／103 張，「逾越」才重新有訊號價值**。（唯二平均低於 650 的組是美股與財報 625、日本 646，那是週末素材薄，不是寫得草率——**不要用字數逼稿**。）

  **`check.py` 自 2026/08/16 起印出兩組字數統計**（一般卡與深度卡的張數、平均字數、逾越區間張數），**但只警告、不計入出口碼**。理由：字數是品質指標不是正確性指標，**為了湊字數注水比超標更糟**；量測的目的是讓漂移被看見，不是拿數字逼稿。區間的權威值在 `check.py` 的 `LEN_REG`／`LEN_DEEP`，本節散文是說明。

**深度卡總量固定 6～8 張，不是每組一張**（2026/08/04 第 9 次修訂）：四個核心組各 1 張為基本盤，再視當日四條主軸補 2～4 張（主軸落在哪組就寫哪組）。**次要組與衛星組原則上不寫深度卡**，除非當天那個主題就是全站最重要的事。子類別由 8 組擴為 15 組後，若還維持「每組 1 張」會變成 15 張深度卡、撰寫量直接翻倍，11:30 前絕對做不完。**寧可 8 張紮實的深度卡配 100 則中長摘要，也不要 15 張注水的。**

1. **三分鐘總覽**：
   - 跨資產快照列（`.snap`）：S&P 500、那斯達克、布蘭特油、黃金、美元/日圓、30 年美債或銅等 6 格（漲綠 `.up`／跌紅 `.dn`／平 `.fl`）。
   - 四張焦點卡（`.focus`）：當日最重要的四條主軸。
   - 七大重點 takeaways（`.takeaways`）：濃縮當日跨版面重點。
   - 溫度條＋情緒註解（溫度條上會依 `thermo.level` 顯示數值標記）、快照列下方的「延續中的主題」列（由 `index.json` 的 `threads` 自動計算連續天數）、主要來源徽章列、本週盯盤時程（`.watch`）。
   - **五資產立場列**（`overview.pulse` 渲染，翻轉時前端自動標記「翻轉·昨偏空」）。
   - **昨日節點回顧**（`overview.watchReview` 渲染在盯盤時程之後，附「N 應驗 · N 落空」統計）。
   - **姊妹庫晨間匯流條**（純前端、只在最新一天顯示）：AI 泡沫監控綜合溫度（讀該站 `<script id="dashboard-data">` 內嵌 JSON）、Podcast 知識庫今晨集數、每日五圖標題。**writer 不用產任何資料**，三站任一掛掉該格自動隱藏。
   - **異常旗標**：快照格在 `index.json` 累積 ≥10 天 `snap` 數值後，當日變動幅度創期間新高會自動標「⚑ 近 N 日最大波動」。純前端計算。
2. **摘要與心得**：讀完當日 25 家後，寫一篇**約 1,000 字**的綜合彙整＋觀點（`.essay`）。要有一句 kick 破題、5～6 段分主題論述（如 AI 資本支出、財報冷熱、能源地緣、央行/債市、亞洲/台股），結尾給「投顧視角小結」與 2～3 個可驗證盯盤節點。非投資建議。
3. **市場總經**（section id＝`macro`，7 組）：**美股與財報**／**央行、利率與匯率**／**台灣**／**中國**／**日本**／**亞太（韓國、印度、東南亞）**／**歐洲**（各分組用 `.group-label`）。
4. **產業與主題**（section id＝`industry`，6 組）：**AI 與半導體**／**金融、併購與企業**／**能源與原物料**／**生技健護**／**信用債**／**黃金**。
5. **政經**（section id＝`politics`，2 組）：**地緣政治（中東與戰事）**／**美國政治與政策**。

**子類別固定為以上十五組。** 要增減必須同時改本節、排程 prompt 與檢查腳本的 `QUOTA` 表，不可只在其中一處新增分組。

### 4.1 分級下限（2026/08/04 第 9 次修訂）

**不是每一組都要 ≥10 則。** 十五組的素材密度差距極大，一律要求 10 則只會逼出注水內容——這正好違背「不為湊數放舊聞或編造」的底線。下限分三級：

| 級別 | 子類別 | 下限 |
|---|---|---|
| **核心**（每天都有大量素材） | 美股與財報、AI 與半導體、台灣 | **≥10** |
| **核心（特調）** | 央行、利率與匯率 | **≥8**（見下方說明） |
| **次要** | 中國、日本、能源與原物料、金融併購與企業、地緣政治（中東與戰事）、美國政治與政策 | **≥6** |
| **衛星**（低頻，重跨期累積） | 歐洲、亞太（韓印東南亞）、生技健護、信用債、黃金 | **≥3**，其中至少 1 則為保底數據卡 |

下限合計 89 則，**全站目標 95～125 則**。

**權威值在 `check.py` 的 `QUOTA`**，本表是它的鏡像；分組名必須與 `QUOTA` 的鍵逐字相符（全形括號與頓號差一個字就會被判「分組名不在 QUOTA 表」）。改任一邊都要同步另一邊。

### 4.1.1 週末模式（2026/08/10 第 16 次修訂）

**判定：窗口起點那一天是週六或週日 → 套用週末下限。** 窗口是「前一日 07:00 → 當日上午」，**主體是前一日**，所以看起點、不看終點（8/08 那輪終點是週六但主體是週五，素材充足 123 則，不該被判為週末）。實務上受影響的是**週日與週一這兩輪**（窗口分別涵蓋週六、週日），週六那輪窗口涵蓋週五、仍算平日。

| 級別 | 平日 | **週末** |
|---|---|---|
| 核心（美股／AI／台灣） | ≥10 | ≥10（不變） |
| 央行、利率與匯率 | ≥8 | ≥8（不變） |
| 次要六組 | ≥6 | **≥4** |
| 衛星五組 | ≥3 | **≥2** |
| 全站則數 | 95～125 | **80～125** |

`scripts/check.py` 會自動依 `window.from` 的星期切換並在輸出標明「模式：平日／週末」，**判定交給腳本**。**權威值在 `WEEKEND_QUOTA` 與 `WEEKEND_RANGE`**，本表是它們的鏡像。

**為什麼要有這個模式**：8/10 那輪窗口涵蓋週日，**Fierce Biotech、FDA、OGJ、IBD、TrendForce 在窗口內全部零新文**，生技組三個指定來源同時開天窗。系統為了湊平日下限開了**四輪補位**，佔全輪成本 **22.6%**，補到的 4 則還全偏公共衛生與健保政策、沒有一則新藥或臨床試驗的硬素材。**週末素材少是事實，不是執行不力——下限跟著現實走，不要讓規則逼出注水內容。**

**核心組不放寬**：美股、AI、台灣、央行這四組週末仍有足夠素材（8/09、8/10 都達標），放寬它們等於無故降低品質。

**這個分級是拿 2026/08/04 的實際素材校準出來的，不是拍腦袋訂的。** 那天全站 91 則，掃過去只有：台灣本地 9 則、日本 7 則、中國 3 則、韓印東南亞 3 則、歐洲 3 則（且都只是順帶提及、不是歐洲主題）、生技健護 3 則、黃金 1 則、**信用債 0 則**。衛星組若訂 ≥10 甚至 ≥6，第一天就會有五組達不到。

**衛星組的價值在於跨期累積，不在單日厚度。** 下游的主題匯流週報與每月 House View 要的是「這個主題連續幾週的軌跡」——黃金一個月累積 40～60 則就足夠寫一頁，不需要每天 10 則。所以**寧可每天穩定 3 則、天天都有，也不要今天 12 則、明天 0 則**。

**保底數據卡**：信用債與黃金即使當天沒有像樣的新聞，也必須各有 1 則數據卡（信用債＝ICE BofA OAS 利差當日值與變動；黃金＝金價、ETF 流向或央行購金）。這不是湊數——**信用利差與 CSP 債務正是 House View 的核心論述之一**，每天記錄一個數字，累積起來就是一條可回溯的曲線。

**歸類原則（拆組後最容易搞混的幾條）**：

- **日圓匯率、BOJ 政策、日本財政 → 日本組**；Fed、ECB、全球利率結構、美元指數 → 央行組。**美日聯合干預這種橫跨兩者的，以「誰是主體」判斷**——日方出手、日本財務省發言就放日本組。（2026/08/04 那天光是日圓干預就有 5 則掛在央行組，拆組後這幾則要移到日本組，否則央行組會被抽空、日本組卻空著。）
- **台股個股與盤勢 → 台灣組**；台灣半導體廠的技術與供應鏈角度 → 視主題可放 AI 與半導體組，不要兩邊都放。
- **信用債組收的是「利差、評等、發債、償債能力」**；銀行股價、併購 → 金融組。
- **黃金組只收黃金與貴金屬**；原油、銅、農產 → 能源與原物料組。
- **生技健護組收新藥、臨床、FDA、藥價政策、醫療服務**；藥廠的純併購財務面可放金融組，但**不要同一件事兩邊各開一張卡**（違反本版內去重）。
6. **關於與方法**：來源、時效、合規、限制（大致固定，日期需更新）。

**每則卡片**：來源徽章＋主題標籤（`.tag`，可 hot/warn/pos）＋日期＋標題（`h3`）＋一段詳細闡述（`.lead`）＋2～3 條重點 bullet（`.card ul`）＋原文連結（真實可點）＋溫度色條（`t-green`/`t-yellow`/`t-orange`/`t-red`）。摘要要「詳細一點、多一些闡述」，不只列點。

---

## 5. 產出與發布流程

1. **先讀 `data/index.json`**，確認今天是否已產出（同日重跑就覆蓋當天的檔）。24 小時窗口制之下今天與昨天完全獨立，**跟昨天唯一要比對的是原文連結去重**（見第 7 點，由 `check.py` 代勞）。
   - **1.5 算出本次窗口**：`from` ＝ 前一日台北 07:00，`to` ＝ 預計寫入時刻。用 `TZ=Asia/Taipei date` 取得，寫進 JSON 的 `window` 欄，並在讀新聞時就拿它當收錄門檻——**在採集階段就篩掉窗口外的文章，不要等到寫卡時才發現要丟掉**，那等於白讀。
2. 用 Claude in Chrome 逐一讀 25 家當日重點（付費為主）。共 **7 個 subagent 分組，但分兩批派工，不要七組同時開**（2026/08/11 第 19 次修訂）：

   | 批次 | 分組 | 為什麼這樣排 |
   |---|---|---|
   | **第一批（4 組）** | **A、C、E、F** | 核心配額與市場數據優先落地；E、F 是歷史上最容易卡分頁的兩組，讓它們在壓力小的時候先跑 |
   | **第二批（3 組）** | **B、D、G** | 第一批**全部回報後**才派。D 組（能源／黃金／AI）最吃分區掃描，獨留較大的分頁空間給它 |

   **理由是實測出來的**：Chrome 分頁群組容量只有約 4～6 格，7 組同時搶會互相回收對方的分頁。8/11 那輪 D／E／F 三組回報失敗率 65～70%，**合計吃掉全輪 58% 的成本卻只交回 44 則**——順的四組每則約 12.5 萬 token，卡住的三組 55.5 萬，差額約佔整輪 45%，而且那天**完全沒有跑補位**（十五組首輪全達標）。**分批看似變慢，但卡住的組本來就跑了 388～451 個回合，消除重試之後總時間不會變長。**

   **每份派工提示 ＝ `scripts/subagent_preamble.md` 全文（共用前言：任務邊界、分頁紀律與熔斷、預篩規則、讀法、合規紅線、回報格式）＋ `scripts/list_timestamps.js` 全文 ＋ `scripts/read_article.js` 全文 ＋ `window.from` 的值 ＋ 該組的來源與配額。** 共用段落已外部化（2026/08/09 第 15 次修訂），**不要在每份提示裡重寫一遍**——9 份提示中那三段原本幾乎逐字重複，佔全輪成本約 5.6%，且會連帶推高每個子代理每回合的快取讀取量。

   **唯一的例外是前言第 3.5 節的「各來源眉角」表**：**只貼該組會用到的那幾列，其餘刪掉**（2026/08/11 起）。那張表有 11 列、涵蓋全部來源，但每個子代理只會用到 2～4 列，而它每個回合都要重讀一次——8/11 七組合計跑了 1,816 個回合，光是重讀自己的指示就花掉 638 萬 token。**其餘各節一律照貼，不要自行刪改。**
**七組的來源分配**（各組負責哪些子類別、交多少，見下方配額表）：
   - A：Bloomberg ＋ WSJ
   - B：FT ＋ NYT ＋ WaPo ＋ **The Economist**（The Economist 的出刊節奏與各版取捨見前言第 3.5 節）
   - C：Nikkei ＋ 華爾街見聞 ＋ CNBC ＋ MarketWatch（**另負責全套市場數據**）
   - D：**Tom's Hardware ＋ OGJ ＋ SemiAnalysis ＋ TrendForce**（**TrendForce 中文站 `trendforce.com.tw` 為主**，報價卡務必標明合約價／現貨價、DDR4／DDR5、顆粒／模組、月度／週度）
   - E：Politico ＋ The Hill ＋ Barron's ＋ IBD（後兩家依第 1.1 節的**內文量標準**實測）
   - F：**鉅亨網 ＋ MoneyDJ ＋ TWSE／公開資訊觀測站**（台股專組）
   - G：**Fierce Biotech ＋ STAT News ＋ Korea Herald ＋ Mint**

   **採集配額表（2026/08/16 第 23 次修訂新增）**

   **配額是「回報則數」，不是成卡數。** 回報之後還要經過跨版去重、一事一卡合併、品質汰除，所以配額一律高於成卡下限——**配額等於下限，就是不留汰除餘裕**（8/06 央行組 10 則剛好達標、零緩衝，成因正是去重後縮水）。

   | 子類別 | 平日 下限 → 配額（誰交多少） | 週末 下限 → 配額（誰交多少） |
   |---|---|---|
   | 美股與財報 | 10 → **12**　A6・E6 | 10 → **11**　A4・E5・C2 |
   | AI 與半導體 | 10 → **11**　D5・A3・B3 | 10 → **10**　A3・B3・C2・D2 |
   | 央行、利率與匯率 | 8 → **9**　A5・C4 | 8 → **9**　A5・C4 |
   | 台灣 | 10 → **12**　F12 | 10 → **11**　F11 |
   | 中國 | 6 → **7**　C7 | 4 → **5**　C5 |
   | 日本 | 6 → **8**　C8 | 4 → **5**　C5 |
   | 能源與原物料 | 6 → **7**　D6・A1 | 4 → **5**　D5 |
   | 金融、併購與企業 | 6 → **7**　B4・A3 | 4 → **5**　B4・A1 |
   | 地緣政治（中東與戰事） | 6 → **8**　E6・B2 | 4 → **6**　E4・B2 |
   | 美國政治與政策 | 6 → **9**　E5・B4 | 4 → **7**　E4・B3 |
   | 歐洲 | 3 → **5**　B5 | 2 → **3**　B3 |
   | 亞太（韓國、印度、東南亞） | 3 → **6**　G6 | 2 → **4**　G4 |
   | 生技健護 | 3 → **6**　G6 | 2 → **3**　G3 |
   | 信用債 | 3 → **4**　A4 | 2 → **3**　A3 |
   | 黃金 | 3 → **3**　D3 | 2 → **2**　D2 |
   | **各組合計** | A22・B18・C19・D14・E17・F12・G12 ＝ **114** | A16・B15・C18・D9・E13・F11・G7 ＝ **89** |

   **這些數字是對齊各組的歷史實交量訂出來的**：平日合計 114、週末 89，都落在近三輪實交（8/13–8/15 為 118／123／122，8/16 週末 110）之內。**成卡下限的權威值仍在 `check.py` 的 `QUOTA`／`WEEKEND_QUOTA`**，本表的「下限」欄是它的鏡像。

   **AI 與半導體改為三組共扛**（第 23 次修訂）：D 的四家裡 SemiAnalysis 週更、OGJ 與 TrendForce 週末不發稿，平日實質只剩 Tom's Hardware 一根柱子，獨扛「能源＋黃金＋AI」三個主題已到承載上限。改由 D、A（Bloomberg 科技與資料中心）、B（FT 科技）分擔。

   **⚠ 分派表必須涵蓋全部十五組，一組都不能漏。** 這條是拿教訓換來的：第 9 次修訂把日圓／BOJ 從央行組移到日本組，卻**忘了指派誰負責「央行、利率與匯率」**——分派表裡它一次都沒出現。結果 2026/08/05 那輪七組回報後央行組只湊到 4 則（下限 10），必須加開一輪補位採集才補到 11 則。**素材從來不缺，缺的是有人負責。** 同一天黃金組也只到 5 則、同樣靠補位，因為 D 組同時扛能源、AI 與黃金三個主題。

   **央行組刻意由兩組分工**，因為它的素材天然分成兩種：
   - **A 組（Bloomberg＋WSJ）＝政策面 ≥5**（配額表同值）：Fed 官員發言、FOMC 前瞻與紀要、政策路徑辯論、其他央行的決策。與信用債同源——利差、殖利率曲線、Fed 路徑本來就是一組論述，交給同一組能省重複閱讀。
   - **C 組（CNBC＋MarketWatch＋華爾街見聞）＝市場數據面 ≥4**（第 23 次修訂由 3 提高，留汰除餘裕）：殖利率曲線變動、美元指數、CME FedWatch 機率、匯率。C 組本來就負責全套市場數據，這是自然延伸。

   **央行組的取材面必須拉寬，光盯 Fed 一定不夠**（2026/08/06 實測：該組 10 則剛好達標、零緩衝，原因是 Cook 談話與財政部再融資被多家媒體重複報導，依「一件事不開兩張卡」合併後可用素材大幅縮水）。**這是結構性問題——央行事件天然會被大量重複報導，去重後留下的獨立事件本來就少，光叫人「多讀」沒有用，要擴大取材面**。該組應涵蓋：

   - **各國央行**，不只 Fed：ECB、BOE、BOJ（政策面歸日本組，但全球利率結構的比較歸這裡）、加拿大央行、瑞士央行、新興市場央行（印度、韓國、巴西、土耳其）
   - **公債標售結果**：美債各天期標售的得標利率、投標倍數、間接投標比重；日債、歐債標售
   - **殖利率曲線與利差結構**：2s10s、5s30s、實質利率與 BEI（損益兩平通膨率）、SOFR 與 OIS 利差
   - **利率衍生品定價**：CME FedWatch 機率變化、OIS 隱含路徑、利率期貨部位
   - **匯率政策**：干預、口頭干預、外匯存底變化、換匯額度（swap line）
   - **財政與貨幣的交界**：財政部再融資公告（QRA）、發債結構、量化緊縮（QT）節奏

   **同一事件的不同角度不可各開一張卡**（那違反本版內去重），但**「同一事件 + 不同市場的反應」可以分開**：例如「Fed 官員鷹派發言」與「30 年殖利率破 5% 後的長天期需求變化」是兩件事，前者是政策、後者是市場結構。

   **派工前核對配額表涵蓋全部十五組**：每一組都要有人，而且有數字。少了誰當場加派或自己補讀。

   **第一批回報後就點數，不要等到寫卡**（第 23 次修訂新增）：A、C、E、F 回報後，把素材按十五組歸類點一次；**任一組低於當日配額，就把缺口寫進第二批（B、D、G）的提示裡一併補**。第二批回報後再點一次，**仍低於「成卡下限＋1」的組直接排補位**。寫卡階段才發現缺額，就只剩一輪補位的空間了。

   **F 組內部分配：鉅亨 ≥4、MoneyDJ ≥3，其餘不限**，外加下一點列的台股官方數據；週末 MoneyDJ 不出編輯稿，改由鉅亨與官方端點補（見前言第 3.5 節）。
   **交不出配額時要在 `run` 欄具名寫出是哪一組、哪一家、卡在哪一步**，不要靜靜地讓其他來源補位。
3. **每日必抓的官方數據**（F 組與 C 組分工）：
   - **台股官方數據一律改走 JSON／CSV 端點，不要再開網頁版表單頁**（2026/08/06 第 11 次修訂，全部經實測驗證）。舊的 `mops.twse.com.tw/mops/web/*` 與 TPEx 網頁版**連續三輪被重導至新版 SPA 而失敗**，已確認是結構性問題，不要再試。

     | 資料 | 端點（`YYYYMMDD` 或 `YYYY/MM/DD` 代入當日） | 回傳 |
     |---|---|---|
     | **三大法人買賣超**（市場合計） | `https://www.twse.com.tw/rwd/zh/fund/BFI82U?dayDate=YYYYMMDD&type=day&response=json` | `fields`＝單位名稱／買進／賣出／買賣差額；`data` 六列（自營自行、自營避險、投信、外資及陸資、外資自營、合計）。**單位是元**，換算成億元再寫卡 |
     | **融資融券餘額**（市場彙總） | `https://www.twse.com.tw/rwd/zh/marginTrading/MI_MARGN?date=YYYYMMDD&selectType=MS&response=json` | `tables[0].data`：融資(交易單位)／融券(交易單位)／融資金額(仟元)。**融資金額單位是仟元** |
     | **櫃買（TPEx）指數收盤** | `https://www.tpex.org.tw/www/zh-tw/afterTrading/tradingIndex?date=YYYY/MM/DD&response=json` | `tables[0].fields`＝日期／成交張數／金額(仟元)／筆數／**櫃買指數**／漲跌。回傳當月至指定日的逐日列，取最後一列 |
     | **上市公司月營收** | `https://openapi.twse.com.tw/v1/opendata/t187ap05_L` | JSON 陣列，欄位含 `公司代號`／`公司名稱`／`營業收入-當月營收`／`營業收入-去年同月增減(%)`。**金額單位仟元** |
     | **上櫃公司月營收** | `https://mopsfin.twse.com.tw/opendata/t187ap05_O.csv` | CSV（UTF-8 含 BOM），欄位與上市版相同 |
     | **法說會日期（主源）** | MOPS 官方**「法人說明會一覽表」** | **前瞻用這個**。8/16 實測一次取得 8/17–8/31 共 109 場（上市 77、上櫃 32），仍為官方來源、非媒體轉引。**⚠ 它以「公告月份」為查詢條件，不是以「開會月份」**——所以查 8 月會漏掉**七月就公告的八月場次**。**一律查當月與前一個月兩次再合併去重**（8/16 執行端自行抓到這個限制） |
     | 法說會日期（備援） | `https://openapi.twse.com.tw/v1/opendata/t187ap04_L` | 上市公司每日重大訊息，**法說會以「`符合條款`＝第12款」發布**。**週末會回傳 0 筆**（8/16 實測），因為它是「當日重大訊息」不是行事曆——這正是主源改用一覽表的原因。`主旨` 形如「公告本公司召開法人說明會之相關資訊」；**注意 `主旨 ` 這個欄位名結尾有一個半形空白** |

     **櫃買指數終於有解了**（8/04–8/06 連續三輪未取得，是唯一長期缺口）。**TWSE OpenAPI 沒有三大法人與融資融券**，那兩項只能走上表的 `rwd` 端點；**也沒有法說會專屬端點**，只能從重大訊息第 12 款過濾。

     **讀法（2026/08/13 修正）**：**不要 `navigate` 到 JSON 網址**——Chrome 會把它當檔案下載而不是渲染成頁面，`innerText` 因此讀不到東西。8/13 那輪 OpenAPI 三個端點（上市月營收、上櫃月營收、重大訊息）就是這樣連續失敗逾五次，月營收被迫降級成媒體轉引。

     **正確做法：先 `navigate` 到「與端點同網域」的任一頁面，再用 `javascript_tool` fetch**——
     ```
     await fetch('<端點網址>').then(r => r.text())
     ```
     拿到字串後自己 `JSON.parse`（CSV 端點直接切行）。這仍然是 Chrome 工具、走使用者的瀏覽器 session，**不違反「不要用 WebFetch／curl／Python」那條規則**（線上驗證步驟本來就是這樣做的）。

     **⚠ 同網域這件事是硬性的，跨子網域也會被擋**（2026/08/14 F 組實測）：要打 `openapi.twse.com.tw` 的端點，就得先停在 `openapi.twse.com.tw` 的頁面上；停在 `www.twse.com.tw` 去 fetch `openapi.twse.com.tw` 會被 CORS 擋掉。**三個網域各自對應**：`www.twse.com.tw`（`rwd` 兩個端點）、`openapi.twse.com.tw`（月營收與重大訊息）、`mopsfin.twse.com.tw`（上櫃月營收 CSV）、`www.tpex.org.tw`（櫃買指數）。同一網域的多個端點可以在同一頁一次抓完，換網域才要重新 navigate。

     8/13 因為 `navigate` 到 JSON 網址（Chrome 當檔案下載）失敗；8/14 改 fetch 後六端點 6/6 全取得、零降級——**但那是 F 組自己摸索出同網域規則才成功的，規格當時沒寫**。
     **卡片的 `url` 直接用上表帶了當日參數的網址**——每天不同，不會撞上跨版去重。8/05 那輪就是因為用了每天相同的表單頁網址而被去重擋下。
     **連兩次抓不到才降級**：改用鉅亨／MoneyDJ 的盤後彙整，**在卡片內註明數據轉引自媒體而非官方**，並在 `run` 欄記錄。
   - 利率：**下次會議的升降息機率**。**主源改為 Investing.com 的 Fed Rate Monitor**（2026/08/14 起）——**CME FedWatch 官網已是跨網域 iframe ＋ 掛 reCAPTCHA，不要再每天先試 CME 再降級**，8/13 與 8/14 連兩輪都是白花回合後轉引。卡片上照樣標明數字來源與擷取時間；**若當天 Investing.com 也不可用，才回頭試 CME 官網**。可與華爾街見聞等媒體轉述的百分比交叉驗證，但**不要只引用媒體轉述**。
   - 能源：**EIA 週報庫存**（週三）、每季**美國財政部再融資公告（QRA）**的日期與規模
   - 匯率：日本財務省的干預實績公布（月底）與 BOJ 貨幣市場初步數據
   - **信用債（第 9 次修訂新增，衛星組保底卡）**：FRED 的 ICE BofA 美國投等債 OAS `https://fred.stlouisfed.org/series/BAMLC0A0CM` 與高收益債 OAS `https://fred.stlouisfed.org/series/BAMLH0A0HYM2`——記錄當日值與較前一日、前一週的變動 bps。**即使當天沒有任何信用市場新聞，這張數據卡也要出。** 累積起來就是 House View 講 CSP 債務與利差時的那條曲線。
   - **黃金（衛星組保底卡，2026/08/14 再次換源）**：金價（現貨與 COMEX 近月**要分開標**）＋下列**免登入**數據源：
     - **SPDR Gold Shares 每日持倉** `https://www.spdrgoldshares.com/usa/`——全球最大黃金 ETF，持倉噸數的日變化是**資金流向**的標準代理指標。**GLD 與 GLDM 兩檔都取**（8/14 實測兩檔皆可得）。
       **日變化自己算，不要靠官網**：官網沒有歷史序列 API（8/16 四路徑實測皆 404）。**噸數就寫在前一版的黃金保底卡裡——讀 `data/<前一日>.json` 取出來相減即可**，反正 index.json 本來就要讀。**週末例外**：官網本身不更新（8/15、8/16 最新一筆都停在 8/14），那是真的沒有新數據，如實寫明資料日即可、不要硬算。
     - **ETF 收盤折溢價**（同頁）——資金是在追價還是退潮，折溢價比持倉更早反應。8/14 的黃金卡就是靠「持倉自 1,025.8 降至 1,023.2 噸＋收盤折價 0.54%」判定資金首見退潮，並據此把 `pulse` 的黃金由偏多調為中性。

     **COMEX 黃金庫存已於 2026/08/14 停止追蹤，不要再試。** 連續三輪（8/12、8/13、8/14）確認取不到：CME 官網只提供二進位 `.xls`（依合規未下載）、金屬頁 `innerText` 僅 5 字元、Daily Bulletin 未列該報表、舊頁 404。**它是「實體交割」面向，與 ETF 流向互補，拿不到確實是缺口**——但保底卡的意義是「每天一定拿得到」，**每天再花回合去撞一個確認拿不到的來源，是純浪費**。日後若找到可用的 HTML 來源（且經實測驗證）再加回來。
     **也不要用 World Gold Council 的 `goldhub/data`**：ETF 流向數字**要求登入**，2026/08/08 那輪依合規紅線未建帳號（處置正確）。WGC 公開頁面仍可作背景引用。**黃金保底卡同樣每天必出。**
4. 依本文件與第 3.5 節的 schema 產生 `data/<今天>.json`，並把今天加進 `index.json` 的 `days`。
5. **直接用檔案工具寫入** `/Users/kenny/advisory-knowledge-hub/data/`。每日產出就是該目錄下的兩個檔。
6. 之後**不需手動 push**：使用者 Mac 上的 launchd 背景程式（`com.kenny.dashpush`，每 3 分鐘）會自動 `git add`＋`commit`＋`push`；GitHub Actions 自動部署到 `https://gundamnboy.github.io/advisory-knowledge-hub/`。
7. **發布前自我檢查**（做不到就不要發）：
   - **每張卡片都有 `ts`，且 `ts` ≥ `window.from`**（程式化比對，不是抽查）
   - **每張卡片的 `date` 與其 `ts` 的台北日期一致**
   - 每個子類別達到第 4.1 節的分級下限（**窗口起點落在週末時套 4.1.1 的放寬值**），十五組一個都不能少，全站則數落在該節的區間內
   - 每張卡片都有真實可點的原文連結
   - **不得與前一版共用任何原文連結**（跨版去重）。**唯一豁免：每日更新但網址固定的官方數據頁**——這些頁面**內容每天都變、只有 URL 相同**，去重規則的本意是「同一篇報導不要連兩天成卡」，對它們並不適用。**豁免網域的權威清單是 `check.py` 的 `DEDUP_EXEMPT`**，要查哪些網域在內就看那裡
   - **本版內部原則上一個 `url` 只成一張卡**，例外見下方「彙整型文章的拆卡規則」
   - **`url` 必須是單篇文章的永久連結**，不得使用列表頁、指數頁、首頁或分類頁
   - **每張卡片的 `src` 值必須在第 1 節的徽章清單內**，否則前端會渲染出沒有樣式的空徽章
   - **沒有「未來的 `ts`」**（晚於 `window.to`）——出現通常代表時區換算錯了，而且錯得很安靜
   - JSON 可被 `json.load` 正常解析、`index.json` 的 `days` 已含今天且由新到舊排序

   **檢查腳本已外部化為 `scripts/check.py`（唯一權威版本），直接跑、不要用眼睛掃：**
   ```
   python3 /Users/kenny/advisory-knowledge-hub/scripts/check.py <今天 YYYY-MM-DD>
   ```
   出口碼 0＝通過、1＝有硬性失敗。`QUOTA`、`SRCOK`、`DEDUP_EXEMPT` 三張表都在腳本裡，**改分組、下限、來源代碼或去重豁免時只改該檔**，並同步第 4.1 節的散文版。腳本刻意設計成「缺 `ts` 也不會中斷」——檢查機制自己安靜失效，比沒有檢查更危險。

   **必須是 0 的五項**：缺/壞 `ts`、逾期、未來 `ts`、`date`/`ts` 不一致、與前一版重複。**另外不可出現任何「★違規」的拆卡，`src` 不在清單必須是「無」，十五組都要 OK 且「缺少的分組」必須是「無」。**

   **`QUOTA` 表是分級下限的唯一權威副本**（第 4.1 節是它的散文版）。改分組名稱或下限時**兩邊都要改**，而且分組名必須逐字相符——`亞太（韓國、印度、東南亞）` 的全形括號與頓號寫錯一個字，腳本就會報「分組名不在 QUOTA 表」。`missing` 那一行是防止整組漏掉：只檢查「有出現的組夠不夠」會漏掉「這組根本沒建」的情況，而那正是新增子類別後最常見的失敗。

   **「疑似列表頁」是警告不是硬性失敗**——它用啟發式判斷（最後一段網址沒有連字號也沒有長數字 ID），TWSE 那幾個 `.html` 表單頁會被標出來但屬正常。看到就人工確認一次：是真的列表頁就把該卡的數據併進別張卡，是官方數據頁就放行。

   **這支腳本刻意設計成「缺 `ts` 也不會中斷」。** 舊版直接寫 `c['ts']`，任何一張漏填就 `KeyError` 讓整支腳本掛掉——而執行者很可能因此跳過檢查直接發布。**檢查機制自己安靜失效，比沒有檢查更危險。**

   **檔案路徑一律用 `REPO` 絕對路徑，不要用相對路徑。** `~/podcast-knowledge-digest` 與本 repo 有 **7 個同名檔案**（`AGENT_BRIEF.md`、`MAINTENANCE.md`、`README.md`、`index.html`、`data/index.json`，以及 `data/2026-07-30.json`、`data/2026-08-02.json`）。若工作階段同時掛了兩個資料夾、而腳本用相對路徑又剛好在錯的目錄下執行，就會讀到另一個 repo 的同名檔。**排程執行時只需要本 repo，不要連 podcast 資料夾。**

   **彙整型文章的拆卡規則（2026/08/03 第 4 次修訂新增）**

   「本週行事曆」「下週前瞻」「市場盤點」這類彙整型文章本來就同時涵蓋多個子類別，硬塞成一張卡反而不合理。因此：

   - **彙整型文章可依子類別拆成至多 3 張卡**，但每張的標題與內文必須聚焦**不同主題**，且**不得有兩張落在同一個子類別內**。
   - **一般報導不適用此例外**，一篇只能成一張卡。
   - 拆卡時每張都要能獨立閱讀，不可只是把同一段摘要複製三份換個標題。

   合規實例（8/2、8/3 兩版的既有做法）：華爾街見聞的週行事曆一篇拆成「美股與財報／半導體／企業事件」三張，各講各的。反例：同一篇 IBD 盤勢報導同時成為「美股與財報」與「AI 與半導體」兩張卡，但講的是同一件事——那就該併成一張。

   **`url` 不可以是列表頁。** 8/2、8/3 兩版都出現 `https://www.bloomberg.com/markets/stocks` 被當成兩張卡的原文連結——那是即時指數頁，內容每天都在變，讀者隔天點進去看不到卡片描述的東西，等於沒有出處。**市場數據類卡片若找不到對應的單篇報導，就把數據併進其他卡片的內文或快照列，不要為它單獨開一張卡。**
8. 等 2–4 分鐘後抓 `https://gundamnboy.github.io/advisory-knowledge-hub/data/index.json` 驗證 `days[0].date` 是今天。若拿到舊內容可能是 CDN 快取，改用 Chrome 以 `cache:'no-store'` 重抓確認。

**時間預算（2026/08/05 第 10 次修訂更新）**：目標 **07:30 開始、12:30 前完成**。

**實測參考**：8/04（8 組制、20 家）09:17 完成；**8/05（15 組制、24 家、且含一輪補位採集）09:27 完成**——採集約 38 分鐘、補位 30 分鐘、撰寫約 25 分鐘。連兩輪都遠早於目標。

**時間一直不是瓶頸，湊足各組下限才是。** 目標暫時維持 12:30 而不收回 11:30，理由有三：週末與清淡日還沒測過、本次又加派了央行與黃金的採集責任、上限提高到 125 會多寫 10 則。**觀察一週後若仍穩定在 09:30 前完成，就收回 11:30**，不要讓寬鬆的目標養出拖延。

- **多出來的**：全站卡片全部要當天新寫，目前為 **95～125 則**。
- **省下來的**：不再需要逐張檢視前一版 90 則舊卡、判斷哪些能改寫、哪些該刪、改寫時還要換來源換數字。這件事過去吃掉的時間比想像中多，現在完全歸零。
- **淨結果仍是變重**，所以採集階段要更有效率：**在讀的當下就用 `window.from` 篩掉逾期文章**，不要讀完才丟。每家的掃標題階段先看時間戳，逾期的連點進去都不必。

進度落後時**依序砍**：(1) 深度卡由 6～8 張降為 4 張，只保留當日最重要的四條主軸各一張；(2) 中長摘要降為 350～450 字（正常區間見第 4 節）；(3) **次要組與衛星組**的閱讀篇數各減三分之一——**核心組（美股、AI、央行、台灣）維持原量**，那四組是全站骨幹。

**不可砍的是**：24 小時窗口（放寬窗口等於整套設計白做）、第 4.1 節的分級下限、市場數據、台股官方數據、**信用債與黃金的保底數據卡**，以及「關於與方法」的 `run` 欄如實記錄。

**補位輪次上限 2 輪**（2026/08/10 第 16 次修訂）。8/10 那輪跑了四輪補位（H、I、J、K），合計約 585 萬有效 token、佔全輪 22.6%，換來 23 則——其中 J 組單則成本 91 萬，是 F 組（台股，16 萬）的 5.7 倍。**第三輪之後的邊際效益已經很低，而且撈到的多半是勉強沾邊的題材。** 兩輪補位後仍有缺額就接受，在 `run` 欄具名記錄。

**若時間到了而某子類別湊不到下限**：先用「前瞻／最新一次」框架補（見第 3 節），仍補不足就**如實在 `run` 欄寫出是哪一組、差幾則、當天為何素材不足**，然後照實發布。**絕對不要回頭撈前一版的舊卡來湊數**——那會讓 24 小時窗口這條規則變成一句空話，而且沒有人會發現。

**衛星組連續多日達不到 ≥3 是設計問題，不是執行問題。** 若歐洲、生技健護、信用債、黃金、亞太任一組**連續一週有三天以上湊不到 3 則**，不要每天硬擠——回頭檢討是不是該把該組併回母組（例如信用債併回金融、黃金併回能源與原物料），並在 `MAINTENANCE.md` 第 5 節記錄觀察結果。**子類別存在的理由是下游用得到，不是版面好看。**

**Barron's 與 IBD 一律照讀，不列入降級項目。** 第 1.1 節已證實兩家內文可完整讀取，它們的美股選股視角是其他來源沒有的。要省時間從深度卡張數下手。

**重要操作禁忌**：不要跑任何 `git` 指令（含 `git status`）。沙箱無網路、且不能刪檔，跑 git 會留下 `.git/index.lock` 鎖檔擋住背景推送。只用 `cat`/`ls`/`grep` 等唯讀指令檢查狀態即可。

---

## 6. 基礎設施備忘

- **Repo（本機）**：`/Users/kenny/advisory-knowledge-hub`（家中 MacBook Pro；放在家目錄下，不要移進 `~/Documents`，因 macOS TCC 會擋背景程式存取受保護資料夾）。掛載於沙箱 `mnt/kenny--advisory-knowledge-hub/`（實際掛載名稱以當下 `ls` 為準）。分支＝`main`。
- **GitHub**：`GunDamnBoy/advisory-knowledge-hub`，GitHub Pages（Source＝GitHub Actions），`.github/workflows/deploy.yml` 自動部署。
- **推送認證**：fine-grained PAT `home-mac push`（只授權此 repo 與 podcast repo、Contents 讀寫），**存於 macOS 鑰匙圈**（`git config --global credential.helper osxkeychain`），**不內嵌於 remote URL、不以明碼存在任何檔案中**。換 token：產新 PAT → 在終端機手動 `git push` 一次、於提示輸入新 token（Username＝`GunDamnBoy`）→ 鑰匙圈自動覆蓋 → 撤舊。**任何情況下都不要把 token 寫進檔案或 remote URL。**
- **背景推送腳本**：`~/.dashpush/auto-push.sh`（有變動就 commit、本機領先遠端就 push）；由 launchd agent `com.kenny.dashpush` 每 180 秒觸發。
- **排程任務**：`advisory-dashboard-daily`，cron `30 7 * * *`（台北時間，一週七天）。另一個任務 `podcast-digest-daily` 的 cron 為 `0 3 * * *`（**03:00，2026/08/03 起由 09:00 改**），兩者刻意錯開以分散 token 用量。
- **Chrome 連線（2026/08/03 確認）**：帳號上同時掛著兩個 Chrome 擴充功能實例，`list_connected_browsers` 會回傳 2 台，且**清單裡的 `name` 一律顯示為 Browser 1／Browser 2，不會反映使用者在擴充功能裡取的名字**，`isLocal` 兩台也都是 `true`，無法用來分辨。對應關係如下：
  - **`8f82131f-7af7-4a5d-a5d7-93677f4e3884` ＝ HOME（家中 MacBook Pro，發布機器，各家訂閱在此登入）→ 一律選這台。**
  - `120b7860-d389-4e1a-9c77-6b590e5a9881` ＝ WORK（辦公室那台，已停用為發布機器，勿使用）。
  - 排程情境**不要呼叫 AskUserQuestion 詢問**，直接 `select_browser("8f82131f-7af7-4a5d-a5d7-93677f4e3884")`。
  - **fallback 規則（2026/08/03 修正）**：若 HOME 的 deviceId 不在清單中，改選「**deviceId 不等於 `120b7860-d389-4e1a-9c77-6b590e5a9881`（WORK）的任一台**」，並在 `run` 欄註明是走 fallback。**不要用「`connectedAt` 最大的那一台」當判準**——實測 WORK 的 `connectedAt` 反而比 HOME 大（WORK 1785740506991 > HOME 1785738401261），照那條規則會穩定選到已停用的辦公室機器。若清單上只剩 WORK 一台，視同無可用瀏覽器，改走免費來源降級路徑並在 `run` 欄明說。
  - 選定後務必做一次可用性測試（讀一篇付費來源文章、確認取得完整內文）再開始正式作業。
- **容量**：每日 JSON 約 200KB，一年約 70MB，GitHub Pages 綽綽有餘，因此**歷史全部保留、不設汰除**。若未來單日檔案明顯變大，優先檢查是不是深度卡寫太多，而不是刪歷史。
- **模式限制備忘**：互動階段能讀 Chrome、但雲端不能直接推 GitHub；背景/排程階段能推 GitHub、但讀不到 Chrome。故付費版必在互動階段產出、由本機背景程式負責推送。

---

## 7. 語氣

精簡、可快速掃讀；摘要詳實但不冗長；心得有觀點但保持中立，是分析不是投資建議。（語言與讀者設定見本文件開頭。）
